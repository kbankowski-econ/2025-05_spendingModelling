# IMF WP — Progress Snapshot (2026-06-23)

Working paper: *"Spending Smarter: A Model of Public Spending Efficiency,
Allocation, and Long-Run Growth."* Distilled from `.sped.md`.

## ✅ Done

**Paper structure & writing**
- Split old §4 *Results* into **§4 Model properties** (transmission, multipliers, sensitivity) and **§5 Policy experiments** (reallocation, efficiency gaps, HC+R&D mix).
- §1 introduction rewritten contribution-led, following the Sahm arc (motivation → question → contribution → methods → three findings → road map).
- §2 restructured: 2.1 Households · 2.2 Production and technology · 2.3 Government · 2.4 Market clearing · 2.5 Balanced growth.
- IMF house style applied (numbers, en-dash ranges, "percent", `\eqref`, Title Case titles, lowercase country terms).
- Current build: **20 pages, clean** (no undefined refs/citations).

**Equations & notation (paper ↔ model aligned)**
- Notation reconciled: Calvo `g1/g2`→`x1/x2`; `Cge`→`Ige`, `Bt`→`Dt`, `SAt`→`St`; `alphaSRD`→`alphaHA`; `zeta`→`alphaG`.
- Marginal-utility/adoption-probability clash resolved paper-side: `ξ→λ` (marginal utility), adoption prob `λ→q` (scale `q₀`).
- Steady state collected into one labelled section; spending instruments and reporting vars grouped.
- Efficiency gaps reparametrized as `(1−eˣ)`: `eGI`, `eGE`, `eGRD`; unused efficiency AR processes dropped.
- R&D enters tech creation as the effective wedge `(1−eGRD)Gᴿᴰ`; raw `Gᴿᴰ` enters the budget.
- Model source cleaned (dead equations/blocks removed); all `_results.mat` byte-identical after re-run.

**Figures & tables**
- Six paper figures regenerated from current data via `invoke` task runner (`pyScripts/2026-06_wp-imf/`).
- **Standard-shocks panel (Fig 1)** finalized as a **5×4 block grid** (Demand / Supply / Labor / Nominal / Fiscal rows), `t`-indexed notation in titles, units explained in the note.
- Fiscal block re-expressed as ratios to **steady-state GDP** (`ydss`): new `pdef_yss`/`Trans_yss`/`dserv_yss`/`by_yss` vars; TFP relocated to variables-of-interest.
- Gov consumption made an explicit instrument (`Gc = Gcss + ydss·epsi_gc`); public debt `Dt` is the residual. Financing is explicit per experiment (debt-financed `exp_*` sims vs budget-neutral reallocation sims with a `−0.01` `epsi_gc` offset).
- **Multiplier table (Table 3)** finalized: `makeMultipliers.py`, split by spending type with AE/EMDE sub-rows (R&D EMDE = `--`), own-spending denominator, financing basis stated.
- Two contribution diagnostics added (`plotContributions`, `investigateContributions`) → `docs/contributions/`.

**Model inventory**
- Repo holds **32 model variants**; the paper uses **21** (9 AE + 12 EMDE). 4 Jamaica models kept for the separate FM-panel project. 28 non-paper models purged 2026-06-19.

**Reviews**
- Literature-consistency review done (`issues/literature-consistency.md`), flagging four calibration/attribution items.

## ⬜ Remaining

**New (2026-06-22) — decision pending**
- ⚠️ **Transfers debt-feedback** — `gamma_d_trans` lowered to **0.01** (uncommitted); solves all 32 models but leaves debt drifting to ≈ +57 ppt by 2050 for the infra shock. Decide: keep `0.01` vs larger value.
- **Debt-reaction dummy** (cleaner fix) — add a 0/1 indicator that switches the transfers debt-feedback **off during the experiment window**, on afterward, so debt still stabilizes long-run without transfers reacting in-horizon.
- **Gov-consumption deflation puzzle** — investigate why the gov-consumption shock is *deflationary* (should be inflationary); trace the Phillips-curve channel (`mc`, real wage, labor, Taylor rule).
- **Full reproducibility from `tasks.py`** — `makeMultipliers.py` is still run by hand; add it (and any other hand-run generators) so `run-all` regenerates figures, contribution panels, AND `multipliersTable.tex`.

**Equations**
- **§4.3 Sensitivity** — only qualitative text; quantitative sensitivities to `α_G`, `μ`, and the efficiency wedges still TODO.
- **R&D timing in eq. (Z)** — *decision pending*: mod lags R&D (`Cgrdeff(-1)`) but paper writes it contemporaneously. To match line-for-line, change to `(1−eGRD_{t-1})G_{t-1}ᴿᴰ`. (Do **not** "fix" `α_HA=0.45` vs code `0.1` — deliberate normalization.)
- **Literature follow-up** — decide whether to fix the four flagged items (ς elasticity, 10-yr lag, α_HA=0.45 "as in CGS", LBD wording).

**Before circulation**
- Remove the now-empty TODO box at the top of `draftPaper.tex` (≈ line 111).
