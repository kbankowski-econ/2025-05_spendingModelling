# IMF WP — session handoff (`docs/2026-06_wp-imf/`)

Working paper: *"Spending Smarter: A Model of Public Spending Efficiency,
Allocation, and Long-Run Growth."* Source: `draftPaper.tex`. This note is a
handoff for AI sessions; **it is not auto-loaded** — read it before editing.
The plan (done / remaining) is first; reference material (build, paper↔model
mapping, structure) is below.

### Working in parallel (git worktrees)
When running several agents at once, give each its own worktree so they never
clobber each other's `draftPaper.tex`, figures, or `_results.mat`:
- Create: `bashScripts/wt-new.sh <name>` → a sibling checkout at
  `../sm-worktrees/<name>` on a fresh branch off `main`. The script rewrites the
  gitignored `+utils/+call/paths.m` so MATLAB/Dynare write into the worktree, not
  the main repo (see project `CLAUDE.md` → *Git Worktrees*).
- One agent per worktree/branch; split the TODO list so tasks don't overlap (e.g.
  one agent on equations, one on figures). Each worktree shares the LFS store but
  has its own working files (~1.5 GB smudged), so remove them when done:
  `bashScripts/wt-rm.sh <name>`.
- Merge each branch back to `main` separately; resolve `draftPaper.tex` conflicts
  by section. Avoid two agents editing the same equation block or figure pipeline
  concurrently — coordinate via the TODO list above before fanning out.

Examples:
```bash
# Two agents, two tasks — one on equations, one on figures
bashScripts/wt-new.sh wp-equations     # → ../sm-worktrees/wp-equations (branch wp-equations)
bashScripts/wt-new.sh wp-figures       # → ../sm-worktrees/wp-figures  (branch wp-figures)

# Point each agent/terminal at its own checkout
#   agent A: cwd = ../sm-worktrees/wp-equations  → edits eq. (Z), §2.2
#   agent B: cwd = ../sm-worktrees/wp-figures    → regenerates efficiency*.png

# When done, merge each branch back and tear the worktrees down
git -C . merge wp-equations            # run from the main checkout
git -C . merge wp-figures
bashScripts/wt-rm.sh wp-equations      # removes worktree + branch
bashScripts/wt-rm.sh wp-figures

# Fork from a non-main base, or keep the branch after removing the worktree
bashScripts/wt-new.sh wp-hotfix some-feature-branch
bashScripts/wt-rm.sh wp-hotfix --keep-branch
```

---

## ✅ DONE (2026-06)
- **Paper restructure: Model properties vs Policy experiments** (2026-06-19)
  - Split the old §4 *Results* into two sections: **§4 Model properties** (`sec:properties`)
    and **§5 Policy experiments** (kept the `sec:results` label so existing cross-refs resolve).
  - §4.1 *Fiscal multipliers* (transmission narrative + new multiplier table `tab:multipliers`
    + literature benchmarks; absorbs the old "Implied multipliers" para 1).
  - §4.2 *Sensitivity* (the diffusion-speed figure `fig:diffusion` relocated here; flags
    α_G, μ, efficiency wedges as further sensitivities).
  - §5.1 Reallocation · §5.2 Efficiency gaps · §5.3 *Combining human capital and R\&D*
    (renamed from "Human capital, R&D, and technology diffusion"; absorbs the old
    "Implied multipliers" para 2 on the EMDE combination).
  - Intro section-listing updated. Build: 18 pages, clean (no undefined refs/citations).
  - **Two stubs left:** (i) `% TODO` in `tab:multipliers` — split infra vs human-capital
    multipliers into separate rows once pulled from model output; (ii) financing basis of
    the reported multipliers not stated (single-instrument debt-financed for §4.1 vs
    budget-neutral reallocation for §5.1 is the intended framing).
- **Literature-consistency review** (2026-06-19) → `issues/literature-consistency.md`
  - Checked paper vs CominGertler2006 / Anzoategui2019 / ChangGomesSchorfheide2002.
  - Structure faithful; flags on calibration/attribution: ς=0.8 adoption elasticity (sources
    use ~0.92–0.95; 0.8 is CG's *creation* elasticity); 10-yr lag (CG=10, Anz=5); α_HA=0.45
    "as in CGS" (CGS μ=0.111 qtrly); LBD attribution (CGS tech is exogenous).
- **Section 2 restructure & notation** (`c877750`)
  - Lead-in reworded to "sets out the model's building blocks in turn, starting from households" (was "presents the model by agent").
  - 2.2 retitled "Production and technology"; *Technology creation and adoption* demoted from a subsection to a run-in paragraph inside it.
  - Government subsection retitled "Government: fiscal and monetary policy".
  - Calvo auxiliaries renamed `g_{1,t},g_{2,t}` → `x_{1,t},x_{2,t}` (frees `g` for growth).
  - Trend growth written as constant `g` (was `g_t`).
  - TODO box at top: resolved notation item removed; frame kept (commented-out `itemize` template) for future issues.
- **Wording & clarifications** (`6ce1360`)
  - Public infrastructure capital "augments private production as a positive externality".
  - Footnote on $\epsilon$ vs $\vartheta$: Anzoategui et al. fold markup + returns-to-variety into one elasticity; here $\epsilon$ sets the markup, $\vartheta$ only the love-of-variety TFP term $A_t^{\vartheta-1}$.
  - Cross-reference from the technology block to the production function noting $\vartheta$ governs the productivity payoff.
- **R&D efficiency in eq. (Z)** (`ef56a0c`)
  - R&D now enters as the effective wedge $(1-e^{GRD}_t)G_t^{RD}/[(1-e^{GRD})G^{RD,SS}]$ (matches the mod: `Cgrdeff` in creation, raw `Cgrd` in budget).
  - §"Closing spending efficiency gaps" text now lists all three gaps $e^{GI},e^{GE},e^{GRD}$ (the three bars in Figs 3/4).
- **Model source cleanup** (`baf7a94`, model-side)
  - Removed all commented-out equations + two `/* */` blocks from `models/model_block.modpart`.
  - Deleted unused `model_block_ext.modpart`.
  - Active equation set unchanged; all `_results.mat` byte-identical (verified).

## ⬜ REMAINING / TODO
- **Equations**
  - (wp-equations) make sure the model equations are consistent with the paper
    - notation
    - steady state at the end
    - efficiency gaps
  - (wp-literature) ~~make sure the model is consistent with the literature~~ — **done**;
    findings in `issues/literature-consistency.md`. Open follow-up: decide whether to fix
    the four flagged calibration/attribution items (ς, 10-yr lag, α_HA=0.45, LBD wording).
  - R&D timing in eq. (Z) — *decision pending, next obvious step*. The mod lags R&D (`Cgrdeff(-1)`, period $t-1$) and lags HC (`H(-1)`); the paper lags $H$ but writes R&D contemporaneously ($G_t^{RD}$). To match the mod line-for-line, change eq. (Z) to $(1-e^{GRD}_{t-1})G_{t-1}^{RD}$. Confirm before changing.
  - HC-in-creation coefficient name: paper uses $\alpha_{HA}$, mod uses `alphaSRD`. The `(1-ρ_A)` factor in eq. (Z) is a *deliberate normalization* — do NOT "fix" the apparent $\alpha_{HA}=0.45$ vs code `0.1` (see project memory `alphaHA-normalization`). A footnote mapping the two names may help readers.
- **Figures**
  - Likely stale: `figures/*.png` committed 2026-06-13, *before* the 2026-06-18 AE/EMDE INF-efficiency re-onboarding (AE 0.351→0.359, EMDE 0.399→0.406; commit `3008835`).
  - No figure-generating script in this folder — pipeline not located this session.
  - Before circulation: find how `efficiencyAE_yd`/`efficiencyEM_yd` (and reallocation/humanCapital/diffusion) are produced and regenerate against the current `figureNumbers*.csv` / `_results.mat`.
- **Before circulation**
  - Remove the TODO box at the top of `draftPaper.tex` (currently empty, left for marking new issues).

---

## Files & build
- **Source:** `draftPaper.tex` (single file). Bib: `references.bib` (style `ecta`). Figures: `figures/*.png`.
- **Compile:** `latexmk -pdf -cd docs/2026-06_wp-imf/draftPaper.tex`
- **If it fails with "Runaway argument … `\@newl@bel`" at `\begin{document}`** that's a corrupted `.aux` from an interrupted run. Fix: `latexmk -C -cd docs/2026-06_wp-imf/draftPaper.tex` then rebuild. (The cascade of "Cannot determine size of graphic" errors is a *symptom* of the bad `.aux`, not a figure problem — the PNGs are fine.)
- Current build: **17 pages, clean** (no undefined refs/citations).

## Model behind the paper
- Equations live in **one shared source: `models/model_block.modpart`** (`@#include`d by all 52 `.mod`). The per-model `<name>.mod` are copies of `models/modelTemplate.mod`; `<name>-macroexp.mod` + `model/json/*.json` are generated; `*_results.mat` are the canonicalized outputs.
- Reproducibility verified: re-running `drivers/runModel.m` (all models) + `drivers/runSimulExport.m` leaves every `_results.mat` and `figureNumbers*.csv` **byte-identical**. Headless recipe: `matlab -batch "cd('<repo>'); iniProject; setenv('MODEL_FILTER',''); run('drivers/runModel.m')"` (benign exit-time segfault; verify via log markers "canonicalizeResults: normalized", "runModel total execution time"). `MODEL_FILTER=JAM` runs a subset.

### Paper symbol ↔ `.mod` variable (essential when editing equations)
| Paper | `.mod` | note |
|---|---|---|
| $Z_t$ (created tech) | `ZZRD` | |
| $A_t$ (adopted tech) | `AAt` | enters production as $A_t^{\vartheta-1}$ |
| $\vartheta$ | `varthetaat` | love-of-variety / TFP exponent |
| $\epsilon$ | `epsilon` | markup elasticity (Calvo varieties) — **distinct from $\vartheta$** |
| $x_{1,t},x_{2,t}$ | `g1`,`g2` | Calvo price-setting auxiliaries (renamed in paper from $g$) |
| $g$ (trend growth, constant) | `ZZss` (gross quarterly) | |
| $\Lambda_t$ | trend level | |
| $G_t^{RD}$ (raw) | `Cgrd` | enters the **budget** |
| effective R&D $(1-e^{GRD}_t)G_t^{RD}$ | `Cgrdeff = effcgrdshock*Cgrd` | enters **tech creation** (eq. Z) |
| $(1-e^{GRD})$ | `effcgrdshock` (SS `eff_cgrd`) | productive fraction |
| $\rho_A$ | `rho_ZZRD` | |
| $\alpha_{RD}$ | `alphaRD` | =0 for EMDEs (innovation channel AE-only) |
| $\alpha_{HA}$ (HC-in-creation, paper) | `alphaSRD` (mod) | **name mismatch**; see TODO |
| infra/HC efficiency wedges $e^{GI},e^{GE}$ | `effshock`/`eff`, `effgeshock`/`effge` | |

## Section 2 structure (current)
2.1 Households · 2.2 Production and technology · 2.3 Government: fiscal and monetary policy · 2.4 Market clearing and equilibrium · 2.5 Balanced growth and solution.

## Section structure (current, post-2026-06-19 restructure)
§1 Introduction · §2 The model · §3 Calibration · §4 Model properties (4.1 Fiscal multipliers · 4.2 Sensitivity) · §5 Policy experiments (5.1 Reallocation · 5.2 Efficiency gaps · 5.3 Combining HC and R&D) · §6 Conclusions · App. A Equilibrium conditions · App. B Spending-efficiency estimates (efficiency-bands figure).

## Figure order (for `\ref`/numbering)
AE/EMDE pairs are combined into single side-by-side figures (subpanels a/b) so they sit
inline with the text; all figures use `[H]` (non-floating, by request — accepts the odd
page-bottom gap rather than letting figures drift):
Fig 1 technology diffusion (§4.2, `fig:diffusion`) · Fig 2 reallocation — (a) AE, (b) EMDE (§5.1, `fig:reallocation`) · Fig 3 spending efficiency — (a) AE, (b) EMDE (§5.2, `fig:efficiency`) · Fig 4 HC + R&D mix (§5.3, `fig:humanCapital`) · Fig 5 efficiency bands by income group (App. B, `fig:efficiencyBands` — 2×2 panels: infra, health, education, R&D; R&D plotted for AEs only; script `pyScripts/2026-04_spendingModelling/plotEfficiencyBands.py`).
Tables: Table 1 parameters (§3, `tab:parameters`) · Table 2 efficiency-gap derivation (§3, `tab:efficiencyGaps` — main-text table; AE median; EMDE = avg(EM,LIC); HC = avg(health,education); rows: infra, health, education, HC, R&D (AE 0.399, EMDE shut down); 2023 medians from the efficiency database; note points to App. B Fig 5; ported from the Jamaica deck's calibration slide, Jamaica column + previous-FM parentheticals removed) · Table 3 multipliers (§4.1, `tab:multipliers`) · Table 4 notation (App. A).

## Writing style — drafting the paper (esp. the introduction)
Follow Claudia Sahm, *Writing Economics Research Papers* (2021), in
`literature/ClaudiaSahm_WritingEcon.pdf` — a style reference for drafting, **not a
citation** (do not add it to `references.bib` or cite it in the text).

**How the introduction should be written.** Each element below is **one paragraph**
in the introduction (and **one sentence each** in the abstract), roughly in order:
1. **Motivation** — why the question matters. Set the stage with *facts*; do not just
   assert that it is important.
2. **Research question** — state one *specific* question ("This paper studies / asks
   whether …").
3. **Contribution** — what answering the question teaches us. **Lead with our own
   contribution**, then place it in the context of others. The contribution paragraph
   *substitutes for* a related-literature section; if one is unavoidable, keep it short.
4. **Methods** — how the question is answered: identification and the key features of
   the approach.
5. **Results** — the main findings, **no more than three**, stated clearly and with
   *numbers*. These are the payoff — take the space to explain them.
6. **Road map** — a short by-section description of the rest of the paper.

**Make contribution and findings shine — they are the most important parts.**

General style rules to apply throughout:
- First sentence of each paragraph makes its main point; read in sequence, the opening
  sentences should tell the paper's story.
- Stick to facts; avoid subjective words ("surprising", "important", "obvious") and
  limit adverbs.
- Emphasize *economic* significance over statistical significance.
- No jargon without first defining it (readers come from other sub-fields).
- Figures/tables must stand alone (no acronyms/jargon/symbols), with labelled axes,
  legible fonts, and legibility in black and white; keep a "money chart" for the
  headline result.
- Do not apologize for what the paper does not do.

*State of the §1 intro (revised 2026-06-19):* now follows the Sahm arc and is
**contribution-led** — motivation → specific question + our contribution (others in
context) → compact model/methods paragraph (the former strand-by-strand lit review
folded in, all citations kept) → three findings → road map.

## IMF Editorial Style Guide (house style)
Reference: `literature/IMF_EditorialStyleGuide_2e.pdf` (2024, 2nd ed.) — IMF house
style for the WP. **Not a citation** (do not add to `references.bib` or cite). Rules
that bear on this paper:
- **Numbers:** spell out < 10, numerals for ≥ 10 (mixed → numerals); always numerals
  for percentages, ratios, decades, currency-with-symbol, and millions and above.
  Use the word "percent" in text, not "%".
- **Ranges & punctuation:** en dash for ranges (2026–31; 6–8 percent). Use the serial
  (Oxford) comma. Hyphenate phrasal adjectives before a noun (long-run output,
  debt-to-GDP ratio, spending-efficiency gap).
- **Cross-references:** capitalize in-text refs to Figure / Table / Section /
  Appendix / Box, but **not** numbered equations or figure panels — "equation (2.10)",
  "panel a" (lowercase). Use `\eqref{}` for equations.
- **Titles:** headline-style (Title Case) for section, figure, and table titles
  (matches CLAUDE.md "Title Case for chart titles").
- **Country terms (lowercase in running text):** advanced economies; emerging market
  and developing economies (EMDEs); low-income (developing) countries. Avoid "advanced
  countries" and "emerging economies". GDP and R&D always as acronyms.
- **Currency:** "$" is the assumed US dollar; use "US$" only to disambiguate. "US",
  never "U.S.".
- **Figures/tables:** Source/Sources line first, then a "Note:" line; titles
  headline-style; must stand alone (no undefined symbols/jargon).

*Compliance status of draft (2026-06-19):*
- Already OK: "percent" word; en-dash ranges; lowercase country terms; GDP/R&D
  acronyms; capitalized Figure/Table/Section/Appendix refs; lowercase "panel a".
- **Fixed:** "ten years"→"10 years"; "emerging economies"→"emerging market economies";
  capitalized "(Eq.~\ref…)"/"(Eqs.~…)"→`\eqref` / lowercase "equations"; all section,
  figure, and table titles set to headline-style (Title Case); `\desc` macro now prints
  "Note:" (was "Notes:").
