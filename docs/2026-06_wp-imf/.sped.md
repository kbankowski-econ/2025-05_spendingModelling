# IMF WP — session handoff (`docs/2026-06_wp-imf/`)

Working paper: *"Spending Smarter: A Model of Public Spending Efficiency,
Allocation, and Long-Run Growth."* Source: `draftPaper.tex`. This note is a
handoff for AI sessions; **it is not auto-loaded** — read it before editing.
The plan (done / remaining) is first; reference material (build, paper↔model
mapping, structure) is below.

### Which models feed the paper (NOT all of them)
`runModel.m` now configures **87 model variants**. The 4 `JAM_*` (Jamaica)
models are kept for the separate FM-panel project and are not used in this
paper; the remaining variants support the paper exercises and diagnostics.

The standard expansion exercises comprise **7 permanent** and **7 persistent
temporary** debt-financed simulations: `Model_HumanCapital_exp_{gc,igi,ige,grd}`
and their `_perm` counterparts for AEs, plus the corresponding three models for
EMDEs. Each raises one instrument (`epsi_gc`/`epsi_igi`/`epsi_ige`/`epsi_grd`)
by 1 percent of GDP with no offsetting consumption cut. The permanent paths feed
the headline transmission figure (`fig:standardShocks`) and multiplier table
(`tab:multipliers`); the AR(1) paths (persistence 0.9) are reported in
Appendix E. No EMDE R\&D simulation is included because the public-R\&D
technology-creation elasticity is zero for EMDEs. The
`postSimul.mod` multiplier is output-per-unit-of-investment, so it prints ±Inf
for the consumption sims; the paper table instead divides by each instrument's
own spending (`pyScripts/makeMultipliers.py`), giving finite values.

**Purged on 2026-06-19:** 28 non-paper models — 13 AE + 14 EMDE variants (e.g.
`eff5y`/`eff10y`, AE `eff30y`/`effcge30y`, `*_al`, `igCge`/`igCgrd`, `VarShare`,
`LAGeff`, `cgrd_eff` (the 40-qtr ramp), bare AE/EMDE `epsieff30y`/`epsieffcge30y`)
plus the standalone `Model_FR_rate` orphan (a separate macro-fiscal model with a
sovereign-risk block, never run by `runModel.m`). They were removed from
`runModel.m`'s `modelList` and their files/dirs deleted. `runPlots.m`'s diagnostic
comparison of the two EMDE efficiency-only models was disabled (those models are
gone). To restore any, re-add its `modelList` row and rerun.

The six figures are built by `pyScripts/2026-04_spendingModelling/plot*.py`,
which read columns `<model>___yd` from `docs/csvFiles/figureNumbers_yearly.csv`
(this is the figure pipeline the TODO below flags as "not located"). The exact
models per figure:

| Fig | Script | Models used (`Model_…` = AE, `EM_Model_…` = EMDE) |
|---|---|---|
| 1 AE reallocation | `plotReallocationAE.py` | `epsi_ig`, `epsi_cge`, `epsi_cgrd` |
| 2 EMDE reallocation | `plotReallocationEM.py` | `epsiig`, `epsicge` (no R&D because the technology-creation channel is inactive for EMDEs) |
| 3 matched spending and efficiency | `plotEfficiencyAE.py` | solid AE 0.25%-of-GDP spending benchmarks: `exp_igi_perm025`, `exp_ige_perm025`, `exp_grd_perm025`; dotted matched AE efficiency: `effgi_perm025`, `effge_perm025`, `effgrd_perm025`; dashed matched EMDE efficiency: `effgi_perm025`, `effge_perm025` |
| 4 EMDE spending efficiency (currently outside draft) | `plotEfficiencyEM.py` | `effgi_perm`, `effge_perm` |
| 5 HC + R&D mix | `plotHumanCapitalIRFs.py` | `epsi_cge`, `epsi_cgrd`, `epsi_cgeCgrd` |
| 6 technology diffusion | `plotDiffusionAE.py` | `epsi_cgeCgrd`, `epsicgrd_cge_adt`, `epsicgrd_cge_limt` |

Distinct AE models (9): `epsi_ig`, `epsi_cge`, `epsi_cgrd`, `epsi_igeff25y`,
`epsi_cgeeff25y`, `epsi_cgrd_eff25y`, `epsi_cgeCgrd`, `epsicgrd_cge_adt`,
`epsicgrd_cge_limt`. Distinct EMDE models (12): `epsiig`, `epsicge`, `epsiiglow`,
`epsicgelow`, `epsiigeff25y`, `epsiigeff25ylow`, `epsiigeff30y`,
`epsiigeff30ylow`, `epsicgeeff25y`, `epsicgeeff25ylow`, `epsicgeeff30y`,
`epsicgeeff30ylow`. The current efficiency figures instead use the five permanent
gap-closure models listed above. Regenerating figures only requires re-running the
models used by the relevant figure.

### Working in parallel (git worktrees)
When running several agents at once, give each its own worktree so they never
clobber each other's `draftPaper.tex`, figures, or `_results.mat`:
- Create: `bashScripts/wt-new.sh <name>` → a sibling checkout at
  `../sm-worktrees/<name>` on a fresh branch off `main`. The script rewrites the
  gitignored `+utils/+call/paths.m` so MATLAB/Dynare write into the worktree, not
  the main repo (see project `CLAUDE.md` → *Git Worktrees*).
- One agent per worktree/branch; split the TODO list so tasks don't overlap (e.g.
  one agent on equations, one on figures). Each worktree shares the LFS store but
  has its own working files (~1.5 GB smudged), so remove them when done:
  `bashScripts/wt-rm.sh <name>`.
- Merge each branch back to `main` separately; resolve `draftPaper.tex` conflicts
  by section. Avoid two agents editing the same equation block or figure pipeline
  concurrently — coordinate via the TODO list above before fanning out.

### Step by step (example: `wp-equations` + `wp-literature`)

**1. Create one worktree per task** (from the main checkout):
```bash
# cwd = /Users/kk/Developer/2025-05_spendingModelling
bashScripts/wt-new.sh wp-equations    # branch wp-equations, ../sm-worktrees/wp-equations
bashScripts/wt-new.sh wp-literature   # branch wp-literature, ../sm-worktrees/wp-literature
git worktree list                     # verify
```

**2. Start one tmux session per worktree** — each runs an agent, detached:
```bash
tmux new-session -d -s wp-equations  -c /Users/kk/Developer/sm-worktrees/wp-equations  claude
tmux new-session -d -s wp-literature -c /Users/kk/Developer/sm-worktrees/wp-literature claude
tmux ls                              # list the running sessions
tmux attach -t wp-equations          # attach; detach with C-b d, switch with C-b s
tmux attach -t wp-literature          # attach; detach with C-b d, switch with C-b s
```
`-s` names the session, `-c` sets its working dir, and the trailing `claude`
is the command it runs. Give each the matching TODO: wp-equations → equations
consistent with the paper (notation, steady state, efficiency gaps);
wp-literature → model consistent with the literature. Kill a finished session
with `tmux kill-session -t wp-equations`.

**3. Let them work.** Each agent commits to its own branch inside its own folder
(`git commit` as normal) — files are physically separate, so no clobbering.

**4. Merge back, one branch at a time** (from the main checkout); merge the
smaller branch first so the second merge's conflicts are smaller:
```bash
cd /Users/kk/Developer/2025-05_spendingModelling
git merge wp-equations
git merge wp-literature                # resolve draftPaper.tex conflicts by section
```

**5. Tear down:**
```bash
bashScripts/wt-rm.sh wp-equations      # removes folder + merged branch
bashScripts/wt-rm.sh wp-literature     # add --keep-branch to keep a branch
```

Variations: fork from a non-`main` base with
`bashScripts/wt-new.sh wp-hotfix some-feature-branch`. Parallel MATLAB runs are
safe — each worktree's `paths.m` points at itself, so `_results.mat` land in
separate trees.

---

## DONE (2026-08)
- **Public-infrastructure trend treatment corrected** (2026-08-16)
  - Retained government-provided public infrastructure as an external productive
    input. Private capital and effective labor therefore keep shares `alpha` and
    `1-alpha`, and adopted-technology profits remain the fixed-markup rent.
  - Revised technology growth to
    `1+gammaa = g^((1-alpha-alphaG)/(vartheta-1))` and aligned the paper,
    glossary, derivation notes, and balanced-growth documentation. The public
    stock is external to each firm but still grows with the common real trend.
  - Re-solved all 87 configured models, verified finite paths and the implied
    technology-growth rate in every result, reran both AE sensitivity sweeps,
    and regenerated the complete figure and table pipeline. Updated all
    result-dependent statements and rebuilt the paper and derivation notes.
- **Government-investment composition recalibrated** (2026-08-16)
  - Preserved the country-group totals for government fixed investment and split
    them using the GDP-weighted functional shares in Appendix C: 67/33 percent
    between infrastructure and human capital for AEs and 69/31 percent for EMDEs.
  - Re-solved all 87 configured models, reran both AE sensitivity sweeps, and
    regenerated the complete figure and table pipeline. Updated the affected
    multiplier, channel-contribution, policy-experiment, and efficiency-gap text.
- **Public human-capital elasticity recalibrated** (2026-08-14)
  - Retained $\mu=0.10$ for AEs and reduced the EMDE value from 0.25 to 0.15.
    The calibration is now tied to the school-quality estimate in Card and
    Krueger (1992), the public-education range and developing-economy benchmark
    summarized by Djaji\'c, Docquier, and Michael (2018), and is described as
    literature-guided rather than directly estimated for the model's capital stock.
  - Re-solved all 31 EMDE configurations, regenerated the full figure and table
    pipeline, revised affected quantitative statements, and rebuilt the paper.
- **Country-group targets aligned with the appendix data** (2026-08-14)
  - Set growth, government consumption, total government fixed investment,
    implicit consumption and labor-income tax rates, and public debt to the
    2000--23 means of the annual GDP-weighted AE and EMDE aggregates in Appendix D.
  - Split total fixed investment into infrastructure and human-capital investment
    using the functional shares in Appendix C; public R&D is unchanged.
  - Re-solved all configured models, reran both sensitivity sweeps, regenerated
    the complete figure and table pipeline, and updated all affected quantitative
    statements in the paper.

## ✅ DONE (2026-07)
- **Full model now nests the canonical NK benchmark** (2026-07-28)
  - Replaced the four-level pinning exercise with seven cumulative equation-level
    simplifications: endogenous technology, human capital, public infrastructure,
    private capital, price indexation, trend growth, and the extended fiscal block.
  - Added a simplification-aware steady-state routine. The terminal model has the
    canonical labor-only technology, Calvo block, Taylor rule, and lump-sum-financed
    government-consumption resource constraint.
  - Added temporary and permanent `Simple5`--`Simple7` variants and updated both
    progressive-simplification figures. Every deterministic simulation converges.
    Dynare's local rank check remains unverified through Step 6 because the
    debt-feedback switch is zero in the experiment's reference exogenous state;
    the canonical fiscal endpoint removes that debt root and passes the check.
  - Verified all 16 endogenous paths shared by `Simple7` and the independently
    coded canonical control. Their maximum discrepancy is below `2.1e-14` for
    both temporary and permanent shocks.
- **No-indexation counterfactual added to the progressive-model exercise** (2026-07-28)
  - Added temporary and permanent variants of the New Keynesian model with
    capital that differ from `Simple3` only by setting price indexation from
    `chi=0.6` to zero. Their steady states are identical.
  - This controlled comparison found that removing indexation changes permanent-
    shock inflation mainly in Q1-Q2. It was subsequently superseded in the figures
    by Step 5 of the cumulative seven-level ladder above.
- **Marginal cost added to the transmission figures** (2026-07-27)
  - Added real marginal cost immediately before inflation in the price-setting row
    of the permanent and temporary transmission figures and their corresponding
    model-simplification figures. Figure notes distinguish its percent deviation
    from the annualized percentage-point deviations used for inflation and rates.
- **Permanent shocks promoted to the Model Properties baseline** (2026-07-27)
  - Replaced the Section 4 transmission figure, multiplier table, and sensitivity
    figure with their permanent-shock versions and updated the headline numbers
    in the abstract and introduction.
  - Made the permanent government-consumption simplification the primary model-
    structure robustness exercise. Consolidated the former AR(1) results in a
    persistent-temporary-shocks appendix.
- **Permanent model-simplification counterpart added** (2026-07-27)
  - Added permanent government-consumption variants for the four progressive
    simplifications and the from-scratch canonical NK benchmark, bringing the
    configured model count to 49.
  - Added `simplifiedGcAEPerm` to the chart pipeline and Appendix E as the
    permanent-shock counterpart of Figure 9. Output remains positive across the
    ladder, while inflation is negative in every variant.
  - Re-solved the seven matching permanent-consumption configurations and
    re-exported the simulation datasets. All models converged and their result
    files were canonicalized before MATLAB's known exit-time crash.
- **Permanent-shock sensitivity counterpart added** (2026-07-27)
  - Extended the AE one-at-a-time sensitivity sweep with a permanent-shock mode
    (`SWEEP_PERMANENT=1`), using the same parameter grids as the headline AR(1)
    exercise and writing separate permanent result files.
  - Added `sensitivityIRF_AEPerm` to the chart pipeline and Appendix E as the
    permanent-shock counterpart of Figure 2. The baseline sweep multipliers match
    the existing permanent-shock multiplier table.
  - Refined endpoint-label precision in both sensitivity figures so all R&D
    elasticity labels fit within the chart boundary.
- **AE/EMDE technology-channel distinction clarified** (2026-07-17)
  - Corrected the calibration presentation to match the implemented model:
    technology creation is inactive for EMDEs, making `rho_A` irrelevant, while
    adoption remains endogenous with `varsigma=0.1`. The implementation sets
    `alphaHA=alphaRD=0`, but the paper reports both as `--` and describes them as
    inactive or not applicable; do not report them as zero in the paper.
  - The structural table and glossary now show `rho_A` as inapplicable and
    report the active EMDE adoption elasticity. No model equation, calibration,
    or simulation result changed.
- **Transfer rule simplified** (2026-07-17)
  - Eliminated the transfer-persistence parameter `rho_T`, which was calibrated
    to zero, from the full and simplified model declarations and equations.
    Lump-sum transfers now respond directly to the lagged debt gap through
    `gamma_d_T`.
  - Updated the paper equation, calibration discussion, common-parameter table,
    and appendix glossary, and rebuilt the 39-page paper without undefined
    references or citations.
  - Re-ran and canonicalized all 44 model configurations. Endogenous paths,
    steady states, and exogenous paths are exactly unchanged relative to the
    model with `rho_T=0`.
- **Calibration section restructured** (2026-07-17)
  - Reorganized the section into calibration strategy and common parameters,
    country-group steady-state targets, productive-spending and endogenous-growth
    parameters, spending-efficiency gaps, and internally calibrated or derived
    coefficients.
  - Added live-generated tables for common parameters and AE/EMDE steady-state
    targets. Narrowed the structural table to the productive-spending channels
    and retained the efficiency-gap derivation as a separate empirical table.
  - The new generators read the committed model results and are part of the
    `invoke run-all` table pipeline. This was a presentation-only change: no
    parameter, model equation, steady state, or simulation result was changed.
  - Rebuilt and visually checked the 38-page paper. All references and citations
    resolve, and the calibration tables fit without new overflow warnings.
- **Nominal government financing rate simplified** (2026-07-17)
  - Eliminated the separate `Rmp` policy rate and the redundant financing-rate
    equation. The Taylor rule now determines `R` directly, which is both the
    monetary-policy rate and the nominal rate on government debt.
  - Removed the unused `rho_RG` parameter and `epsi_spread` shock, and updated the
    paper, glossary, export pipeline, response figures, and internal derivation notes.
    The nominal block now reports one nominal rate rather than two identical series.
  - Re-ran and canonicalized all 44 model configurations. Steady states are exactly
    unchanged; the largest difference across common endogenous paths is `1.60e-13`,
    and the retained `R` path differs from either former rate series by at most
    `1.11e-15`. All 1,129 retained columns in both simulation exports are exactly
    unchanged; only the 44 duplicate `Rmp_ann` columns were removed. Regenerated the
    affected figures and rebuilt both the 38-page paper and the model-derivation document.
- **Trend growth simplified to a calibrated constant** (2026-07-17)
  - Replaced the dormant endogenous process for `g` with the calibrated parameter
    `g`; removed `epsi_g`, `rho_g`, and the growth-process equation. Stationarized
    intertemporal and accumulation equations now use the constant `g` directly.
  - Updated the balanced-growth discussion, glossary, and internal derivation notes.
    The technology growth factor remains derived as
    `1+gammaa = g^((1-alpha-alphaG)/(vartheta-1))` after accounting for the
    trend growth of public infrastructure.
  - Re-ran and canonicalized all 44 model configurations. Every shared steady state
    is exactly unchanged, and the maximum absolute difference across all common
    endogenous paths is `2.13e-13`. The pre-existing stochastic rank-condition
    warning remains, while every deterministic perfect-foresight simulation converges.
    Re-exported both simulation datasets and rebuilt the 38-page paper successfully.
- **Technology-creation persistence normalization restored** (2026-07-16)
  - Rewrote equation (Z) with explicit $(1-\rho_A)\alpha_{HA}$ and
    $(1-\rho_A)\alpha_{RD}$ terms in the model, main text, and appendix.
  - Recalibrated the long-run elasticities to
    `alphaHA=0.1/(1-rho_A)` and `alphaRD=0.09`, leaving the effective
    per-period loadings at 0.10 and 0.0189 and therefore preserving all model
    results.
  - Re-ran all 44 model configurations and compared them with commit
    `9164daf1`: endogenous paths, steady states, and effective technology
    loadings are identical (maximum absolute difference 0). The full AE
    sensitivity sweep likewise reproduces all prior multiplier and IRF values
    exactly after translating the R\&D-elasticity grid to the new normalization.
- **Auxiliary dummy added to the transfer rule** (2026-07-16)
  - Added the deterministic binary variable `eTaux`, denoted
    $e_{T,t}^{\mathrm{aux}}$ in the paper, directly to the debt-feedback term.
    Setting it to zero suspends the transfer response to debt.
  - Set it to zero over quarters `1:1000` in all 18 non-budget-neutral `*_exp_*`
    models, covering the full shock path and every reported horizon; it returns
    to one afterward so transfers restore debt stability. Budget-neutral reforms
    keep the dummy at one.
  - Re-ran all 44 models. Steady states are exactly unchanged, budget-neutral
    paths are byte-for-byte numerically unchanged, and the largest change in a
    non-fiscal endogenous path is `1.03e-13`. Fiscal paths change as intended:
    transfers are flat during the experiments and the deficits accumulate in debt.
  - Re-expressed the switch as a direct multiplier of the debt-feedback term;
    all 44 steady states and endogenous paths are exactly unchanged relative to
    the preceding `(1-debtfin)` implementation.
- **Tax-rate rules simplified to direct shocks** (2026-07-16)
  - Replaced the dormant AR(1)-plus-debt-feedback rules with
    `tauc=taucss+epsi_tauc` and `tauw=tauwss+epsi_tauw`; a shock of `0.01` is a
    one-percentage-point tax-rate change, and any persistence is set in its shock path.
  - Removed the four obsolete parameters `rho_tauc`, `rho_tauw`, `gamma_d_tauc`, and
    `gamma_d_tauw` from both declarations, the common calibration, and the glossary.
  - Re-ran all 44 models: steady states are exactly unchanged, the maximum absolute
    difference across all simulated endogenous paths is `1.39e-13`, and both exported
    CSV datasets are byte-identical. The canonical MAT files changed because their
    model metadata and eigenvalue structures correctly reflect the simpler equations.
- **Fiscal and monetary policy subsection restructured** (2026-07-16)
  - Opens with the fiscal/monetary division of responsibilities, then presents the
    government budget constraint before discussing individual policy instruments.
  - Separates spending instruments, productive conversion and efficiency, tax rules,
    transfer-based fiscal closure, experiment financing, and monetary policy into a
    deliberate sequence. The tax and transfer rules are now stated explicitly.
  - Corrected the borrowing-rate persistence term from `rho_RG*R(-1)` to
    `rho_RG*log(R(-1))` in the shared full and simplified model blocks, the paper, and
    glossary. All 44 canonical result files and both simulation CSV exports remain
    byte-identical because the baseline sets `rho_RG=0`.
  - Regenerated the glossary and model artifacts. Paper build: 38 pages, with no
    undefined references or citations and no new overflow warning in the subsection.
- **Detrending elaboration consolidated into §2.5** (2026-07-02) — App. A's intro no
  longer restates the Λ/g/1+γ^a machinery; it says level-form-except-technology-block in
  one sentence and points to `subsec:bgp`. §2.5 gained the closing note that App. A's
  technology block is stated directly in stationarized form (S_t relative to trend).
  The `eq:Astat` follow-up sentence shrank to a pointer. See the updated "Appendix A
  presentation conventions" below.
- **Factor-price markup wedge fixed + SDF/ε^q exactness (App. A/B pass, round 2)** (2026-07-02)
  - **Substantive:** the paper's factor demands omitted the `markupss` wedge — model prices
    factors off `mc/μ^p` (`model_block.modpart:46`, `Steady_states_solution.m:34`), and the
    residual profit `(μ^p−1)/μ^p·mc·Y` is what funds the technology values in `eq:VA`.
    Fixed paper-side with the minimal two-layer framing (intermediate producers at fixed
    markup μ^p=1.18; Calvo *retailers* with ε=10 buy at `mc`): `eq:factordemand` + App. A
    derivation carry `mc_t/μ^p`; §2.2 "final goods" aggregates *retail* varieties; price
    setting attributed to retailers; ε-vs-ϑ footnote names both markups. The old
    `issues/equation-reconciliation.md` ✅ for factor FOCs was WRONG — corrected there with
    a full write-up.
  - **SDF exact:** `eq:sdf` now the tax-adjusted form = β·C_t/C_{t+1} identically
    (matches `model_block.modpart:29`); caveat dropped; §2.2 mention now β·C_t/C_{t+1}.
  - **ε^q shown:** App. A `eq:probadopt` now `q_t=(q_0+ε^q_t)S_t^ς` (zero in all
    experiments), matching the model and glossary.
  - **Glossary (`makeGlossary.py` + regenerated):** `rk`/`mc` equations carry the wedge;
    `markupss` got paper symbol μ^p (was `--`); `q` equation carries ε^q; `byss` symbol
    fixed `$d^*$`→`$b^*$`; `rk`/`w` AND `SDF` moved from the Households group to
    Production and technology. **Grouping criterion (user-set): a variable goes where
    the PAPER derives its defining equation** — not where the model file happens to
    place it (that layout is arbitrary). `rk`/`w`: firm factor demands (§2.2/App. A
    production). `SDF`: defined at `eq:sdf` in App. A's Production and Technology
    block, placed right before the value functions V/J it discounts.
  - The borrowing-rate equation was subsequently corrected to use `log(R(-1))` throughout.
    The correction is inert at `rho_RG=0` and dimensionally consistent for `rho_RG>0`.
- **Appendix A/B refinement pass** (2026-07-02)
  - **Fixed swapped Calvo auxiliaries in App. A prose:** $x_{1,t}$ discounts marginal
    **cost**, $x_{2,t}$ marginal **revenue** (the text had them reversed; verified against
    `model_block.modpart:38–40` — `x1` carries `mc`, `x2` carries `PIstar`).
  - Instrument rules (`G^C`/`I^GI`/`I^GE`/`G^RD`) collapsed from a 2-row `align` (2 equation
    numbers for 4 rules) to a single numbered `equation`+`aligned` 2×2 block.
  - The level-vs-stationarized hybrid and the two forms of the adoption probability are now
    flagged in the App. A intro and after `eq:probadopt` — see "Appendix A presentation
    conventions" below (deliberate, not TODOs).
  - Market-clearing: the adoption-cost term is now explicitly tied to $\mathcal{R}_t$ in
    `eq:resource`; §2.4 corrected — $\mathcal{R}_t$ is the **adoption** cost only (creation
    is $G^{RD}$, a separate demand term; verified vs `model_block.modpart:123`).
  - Prose: long sentences split (household setup + FOC discussion, factor demands, App. B
    endogenous-table intro); fiscal "where" clause reworded ("variables without a time
    index denote steady-state values"); App. B shocks subsection got an intro line (all
    shocks zero in steady state).
  - Debt notation audited: paper uses $B_t$/$b_t$ throughout, no $D_t$ — deliberate (one
    bond instrument), see conventions note below.
- **Stationarization / detrending fully documented** (2026-07-02)
  - **§2.5 "Balanced Growth and Solution"** (`subsec:bgp`, label added) now explains the
    level→stationarized transformation: trending variables scaled by the technology trend
    $\Lambda_t$, the gross factor $g \equiv \Lambda_t/\Lambda_{t-1}$ surfacing wherever
    variables enter at different dates, worked bond-Euler and capital-LoM examples, and the
    technology-stock exception ($1+\gamma^a$). Investment is scaled by the **incumbent** trend
    $\Lambda_{t-1}$ (so the capital LoM is $g_t\hat K_t=(1-\delta)\hat K_{t-1}+\hat I_t$, matching
    the code `Kp*g = (1-delta)*Kp(-1)+Ip`) — an earlier blanket "everything /$\Lambda_t$" claim
    that contradicted the model was corrected.
  - **Appendix A (Equilibrium Conditions) intro refined** — states the level-form derivation +
    stationarized solution and cross-references `subsec:bgp`.
  - **New investigation note `investigations/detrending/`** (`detrending.tex`/`.pdf`,
    self-compiling): worked capital-LoM derivation; where $g$ appears in the model vs why
    $\Lambda$ never does; why the **human-capital stock carries no $g$** (stationary vs trending
    stocks); why the **SDF carries no $g$** (stationary kernel, growth carried at the point of
    use — $g_{t+1}$ in the Euler, $1/(1+\gamma^a)$ in the adopter values, $1/g$ in the budget);
    the investment-scaling convention.
- **Technology-block growth factor fixed: $g \to 1+\gamma^a$** (2026-07-02) — *paper-side only; the model was already correct.*
  - The appendix technology block (adoption LoM `eq:Astat`, value functions `eq:VA`/`eq:JZ`, and
    the $S$-FOC `eq:SAfoc`) wrote the detrending factor as $g$, but the model detrends the
    technology stocks $Z,A$ by their own rate `(1+gammaa)`
    $=g^{(1-\alpha-\alpha_G)/(\vartheta-1)}$. Technology grows faster than output because it enters
    production only via $A^{\vartheta-1}$; a single trend at $g$ would leave production unbalanced.
  - **Fixed:** the four appendix equations, the glossary $A/\mathcal V/\mathcal J/S$ equations, and
    §2.5 now use $1+\gamma^a$; `gammaa` gets the paper symbol $\gamma^a$ (was "--"). Documented in
    `investigations/tech-growth-rate/` and `issues/equation-reconciliation.md` ("Technology-block
    detrending — FIXED").
- **`draftPaper.tex` reformatted to one sentence per line** (2026-07-02)
  - Each prose sentence on its own source line for cleaner diffs / easier proofreading. **Pure
    formatting — the compiled PDF is byte-identical** (893,542 bytes), 35 pages. The reformatter
    protects inline math and abbreviations (`e.g.`) and skips math/tabular/verbatim environments.

## ✅ DONE (2026-06)
- **Appendix D restructured into a "Permanent Shocks" section + new transmission figure** (2026-06-30)
  - Appendix D retitled **"Permanent Shocks"** (`app:permmult`) and split into two subsections:
    **Transmission** (new `fig:standardShocksPerm`) and **Fiscal Multipliers** (the existing
    permanent-multiplier table `tab:multipliersPerm`). Sets the slot up to become the eventual
    permanent-shocks subsection carrying both IRFs and multipliers.
  - **New figure `fig:standardShocksPerm`** — the permanent-shock counterpart of Fig 1, same 5×4
    block grid, built by `pyScripts/2026-06_wp-imf/plotStandardShocksAEPerm.py` (copy of
    `plotStandardShocksAE.py` with SERIES pointing to the `_perm` models, `OUTPUT_STEM=
    "standardShocksAEPerm"`). Registered in `chartTable.csv` (`fig_standardshocks_ae_perm`),
    `tasks.py` (`plotStandardShocksAEPerm` task + `run_all`).
  - **`runSimulExport.m` now exports the 7 `_perm` models** — added
    `Model_HumanCapital_exp_{gc,igi,ige,grd}_perm` (AE) +
    `EM_Model_HumanCapital_exp_{gc,igi,ige}_perm` (EMDE) to the manual modelList (line 18),
    so `figureNumbers{,_yearly}.csv` carry their columns. The EMDE `_perm` columns are now
    present too — ready for a future EMDE permanent figure. These models already had
    `_results.mat` (created for the permanent-multiplier table), so no `runModel.m` re-run.
  - Clean build (two passes, no undefined refs); committed `4998d6d` (figure) + `a416144` (pdf).
- **Removed Figures 9 & 10 (duration + persistence) and all related material** (2026-06-26)
  - Dropped `fig:durationGc` and `fig:persistenceGc` from the paper and the project:
    deleted `plot{Duration,Persistence}GcAE.py`, their chartTable rows, the `tasks.py`
    tasks + `run_all` entries, the figure files, and the **7 NK variant models**
    (`Model_NK_exp_gc_{d1,d4,d20,ar0,ar50,ar90,ar99}`). `Model_NK_exp_gc` is now AR(1)
    ρ=0.9 (Figure 8's canonical-NK line; Fig 8 plot repointed to it). Model count
    44 → **37**. The φ-sweep that motivated those figures still lives in
    `investigations/galiCheck/`.
  - **Appendix C reduced to structure-only robustness:** retitled "Government
    Consumption: Robustness to Model Structure"; intro/Fig-8 prose reframed to a single
    point — the inflationary sign is a property of the New Keynesian structure (robust
    across the simplification ladder), not of growth/capital features. The
    duration/persistence narrative is gone. Paper now **22 pages**, clean.
- **Frisch elasticity recalibrated to Galí's value (φ=5) — whole paper** (2026-06-26)
  - Changed `phi` 1.2 → **5** (Frisch 0.2, Galí 2015 Ch.3) in BOTH
    `models/parameters_common.macro` (full model, all sims) and
    `models/modelTemplateNK.mod` (NK benchmark). Re-ran all 44 models, re-exported,
    regenerated every figure + table (`invoke run-all`). Documented in §3 with a new
    `\citet{gali2015}` (added to `references.bib`); **Table 1 has no Frisch row** (its
    `$\phi$`=0.98 is the technology survival rate `phiob`, unrelated to the Frisch `phi`).
  - **This flips the deflation result.** With the standard (inelastic) Frisch the
    gov-consumption shock is **inflationary** at AR(1) ρ=0.9 (canonical NK impact
    +0.35; full-model Fig 1 gc +0.03→+0.15). Deflation now appears **only for
    permanent / near-permanent (ρ=0.99) shocks** (NK permanent ≈ −0.57, about half
    the old −0.9). So the earlier "even canonical NK deflates" finding (see the
    2026-06-25 deflation-puzzle entry below) holds **only for permanent shocks +
    elastic labor**; under Galí's Frisch + realistic persistence it does not.
  - **Appendix C rewritten** to match: `fig:simplifiedGc` (Fig 8, now all AR(1) ρ=0.9 —
    Simple1-4 switched to AR(1), NK line repointed to `Model_NK_exp_gc_ar90`) shows the
    response is **inflationary across the whole simplification ladder**; `fig:durationGc`
    (Fig 9) short shocks inflationary, only 5y/permanent deflate; `fig:persistenceGc`
    (Fig 10) inflationary up to ρ=0.9, deflationary only at ρ=0.99 (**no longer monotone**).
    Figure notes + chartTable registry notes updated.
  - ✅ **Editorial flag — RESOLVED (2026-06-26):** Figs 9 & 10 removed and Appendix C
    reframed to a structure-only robustness check (see the DONE entry above). The
    duration/persistence claims here are historical — the paper no longer makes them; the
    φ-sweep behind them lives in `investigations/galiCheck/frisch_rho09.m`.
- **Standard expansions → AR(1) ρ=0.9** (2026-06-26)
  - The 7 standard expansion sims (`Model_HumanCapital_exp_{gc,igi,ige,grd}` AE +
    `EM_Model_HumanCapital_exp_{gc,igi,ige}` EMDE) changed from **permanent** to **AR(1)
    persistence 0.9** shocks (1%-of-GDP impact; spec `'ar1',[0.01 0.9],'1:1000'` in
    `runModel.m`). Re-ran + re-exported; regenerated `fig:standardShocks` (Fig 1, AE) and the
    whole `tab:multipliers` (AE+EMDE), and updated the figure/table prose, §4.1 intro, the
    chartTable note, and the `makeMultipliers.py` docstring. AE Gc 25y multiplier shifted
    0.97→−0.32, EMDE Gc 25y 0.79→−0.00; investment multipliers also moved. Both country
    groups now share the AR(1) ρ=0.9 convention (done in two steps: AE first, then EMDE for
    table consistency). 26 pp, clean build. The appendix deflation figures (Figs 8–10) and
    the §5 reallocation experiments are unchanged (still their own shock specs).
  - Every paper result is now produced by an `invoke` task; no hand-run generators remain.
    The four `\input` tables each get their own task, all added to `run_all`:
    `makeMultipliers` (Table 3, from `_results.mat`), `makeParametersTable` (Table 1),
    `makeEfficiencyGapsTable` (Table 2, recomputed live from the efficiency database —
    same source as `fig:efficiencyBands`), `makeNotationTable` (Table 4). The table
    generators live in `docs/2026-06_wp-imf/pyScripts/`; `run-all` regenerates figures,
    contribution panels, AND all table `.tex` files. (Table 1's values are still encoded,
    not read from `.mat` — see the REMAINING "Table 1 parameter reconciliation" item; that
    is a data-sourcing choice, not a reproducibility gap.)
  - ✅ **Gov-consumption deflation puzzle — RESOLVED (2026-06-25).** The deflation is the
    standard New Keynesian *wealth effect on labor supply* (Galí–López-Salido–Vallés 2007's
    motivating puzzle), not a bug or a non-standard-feature artifact. A permanent, wasteful,
    lump-sum-financed `Gc` increase lowers lifetime resources → households work more, the
    real wage = marginal cost falls → the NKPC delivers deflation. Demonstrated by three
    appendix figures (App. C, all on the from-scratch canonical NK `Model_NK_exp_gc*`, built
    from `modelTemplateNK.mod`; plotted by `pyScripts/2026-06_wp-imf/plot{Simplified,Duration,
    Persistence}GcAE.py` from the **quarterly** `figureNumbers.csv`, same 5×4 block grid):
    · `fig:simplifiedGc` (Fig 8) — full model → 4 simplifications → canonical NK; deflation
      survives every step, so it is a textbook-NK property under a permanent shock.
    · `fig:durationGc` (Fig 9) — on/off shocks of 1q/1y/5y/permanent (`_d1`/`_d4`/`_d20` +
      permanent). Short shocks are *inflationary* on impact (small wealth effect → demand
      dominates); the permanent one gives the full −0.92 ppt deflation.
    · `fig:persistenceGc` (Fig 10) — AR(1) shocks `ρ ∈ {0,0.5,0.9,0.99}` (`_ar0/_ar50/_ar90/
      _ar99`); inflation impact +0.09 (ρ=0) → −1.05 (ρ=0.99), monotone in ρ. Sign flips
      between ρ=0.5 and ρ=0.9.
    Tooling added: an **`ar1` shock kind** in `+utils/+subroutines/generateShocksFile.m`
    (spec `{var,'ar1',[impact rho],'1:N'}` → geometric path `impact*rho.^(0:N-1)`, periods
    enumerated). The 8 `Model_NK_exp_gc_{d1,d4,d20,ar0,ar50,ar90,ar99}` models are in
    `runModel.m`/`runSimulExport.m`; `mc`/`W_real` were added to the export `varList`. These
    NK benchmarks are NOT in the paper's model count — they are appendix-only illustrations.
  - ✅ **Canonical NK = Galí (2015, Ch. 3) — VERIFIED numerically (2026-06-25).** Built a
    cross-check in `docs/2026-06_wp-imf/investigations/galiCheck/` (see its `README.md`):
    `gali_g.mod` = Pfeifer's Galí Ch. 3 nonlinear FOCs **+ a wasteful, lump-sum-financed
    government block** `Y=C+G`; `ours_g.mod` = our `modelTemplateNK` with Galí's calibration;
    `compare.m`, `phi_sweep.m`. Driven by an identical AR(1) `Gc` shock under perfect
    foresight. **Result: with α=0 the two models agree to max |percent-dev diff| = 1.3e-5
    over 60 quarters** (≈ machine/solver precision) for every variable — our NK is
    equation-for-equation Galí Ch. 3 plus government consumption (the SGU-style recursive
    Calvo form and the `omega` labor-weight normalization are algebraically equivalent). The
    Galí baseline DRS case (α=1/4) differs only in the *magnitude* of the real-wage /
    marginal-cost / hours response (steeper, decreasing returns) — **same signs**.
  - ⚠ **Key finding — the deflation's sign is calibration-dependent (Frisch × persistence).**
    The cross-check exposed that under **Galí's own calibration** (φ=5, i.e. Frisch 0.2) a
    *persistent* AR(1) `ρ=0.9` `Gc` shock is **inflationary** (+0.11 impact), not deflationary.
    Sweeping φ in the verified model (all else at Galí) gives impact inflation (percent dev):

    | φ (Frisch) | ρ=0.9 | ρ=0.99 |
    |---|---|---|
    | **5.0 (0.20) — Galí** | **+0.114** | −0.029 |
    | 2.0 (0.50) | +0.041 | −0.076 |
    | **1.2 (0.83) — ours** | −0.003 | −0.108 |
    | 0.5 (2.0) | −0.068 | −0.159 |
    | 0.2 (5.0) | −0.110 | −0.195 |

    So the sign is governed **jointly** by (i) persistence — more persistent ⇒ more
    deflationary (matches Figs 9–10) — and (ii) the Frisch elasticity — more elastic labor
    (lower φ) ⇒ more deflationary. The deflation needs the wealth effect to actually shift
    labor supply, which requires both elastic labor *and* a large PV of the spending. At
    Galí's textbook Frisch (0.2) a typical transitory stimulus is inflationary; deflation
    appears only near-permanent. The paper's "even canonical NK deflates" claim is correct
    **for the permanent shock** (used in `fig:standardShocks` + the simplification figure)
    but is **not universal**. Our calibration (φ=1.2, plus `γy=0.25`/`θ=0.8`) sits on the
    deflationary side; pure Galí does not at ρ=0.9.
  - ✅ **ρ=0.9 Gc shock under a Galí-like Frisch — DONE (2026-06-26).** In the PAPER's
    calibration (`investigations/galiCheck/ours_paper.mod` + `frisch_rho09.m`, the
    `fig:persistenceGc` model), at `ρ=0.9` the deflation is driven by the high Frisch
    elasticity: φ=1.2 (Frisch 0.83) → **−0.29** ann-pp inflation; φ=5 (Galí's Frisch 0.2)
    → **+0.35** (inflationary); sign crosses zero around Frisch≈0.4 (φ between 2 and 3).
    The φ=1.2 run reproduces `fig:persistenceGc`'s ρ=0.9 inflation (−0.29) exactly. **But a
    near-permanent ρ=0.99 shock stays deflationary even at Galí's Frisch (−0.41 ann pp).**
    Takeaway: the permanent-shock deflation is robust; the ρ=0.9 deflation hinges on the
    paper's (relatively elastic) labor supply. Full numbers + IRF overlay in
    `investigations/galiCheck/README.md` / `frisch_rho09.png`.
- **Standard-shocks panel finalized + transfers-rule experiments** (2026-06-22)
  - `fig:standardShocks` evolved from the 4×4 grid (see 2026-06-21) to a **5×4 block
    grid** (15×18.75 cm), one labelled block per row with a vertical uppercase grey
    chip on the left (DEMAND / SUPPLY / LABOR / NOMINAL / FISCAL):
    · **Demand** $Y^d_t, C_t, I_t, G_t$ ($G$ is the single total-spending variable).
    · **Supply** = the production-function inputs $A_t, K^{GI}_t, K_t, N_t$ (dropped the
      reporting-only TFP; `Kg` and `N` added to the export `varList`).
    · **Labor** (new row) decomposes effective labor into $L_t$ and $H_t$; the other two
      slots are intentionally blank (axes hidden).
    · **Nominal** inflation, nominal interest rate, real interest rate (annualized ppt).
    · **Fiscal** primary deficit, transfers, debt-to-GDP (debt-service panel removed).
  - Panel titles carry the paper's `t`-indexed notation ($Y^d_t$, $R^{mp}_t$, $d_t$, …)
    via HTML sup/sub; **y-axis unit labels removed** — the figure note now spells out how
    each block's deviations are computed. Legend-to-plot gap widened (top margin 86 px).
  - **Fiscal block re-expressed as ratios to steady-state GDP** (`ydss`, not current
    `yt`): new model variables `pdef_yss`/`Trans_yss`/`dserv_yss`/`by_yss` (replacing the
    old `/yt` `_pp` series), `TFP` relocated into the variables-of-interest block. With a
    constant denominator the panels show pure debt accumulation rather than the
    output-deflated ratio (commits `71e46e7`, merge `a641307`).
  - **Transfers-rule parameter experiments** — transfers are the *only* debt-stabilizing
    channel (tax rates have no debt-feedback term):
    · `rho_trans` 0 → 0.9 (more persistent transfers) tried, then **reverted to 0**.
    · `gamma_d_trans` lowered 0.5 → **0.01** so transfers barely react. Tested on
      `Model_HumanCapital_exp_igi`: stability floor ≈ 0.0015 (`0.001` fails the rank
      condition, `0.002`+ solve); `0.01` solves all 32 models with margin. **Tradeoff:**
      the weak feedback leaves debt nearly unstabilized within the horizon — debt-to-GDP
      drifts to **≈ +57 ppt by 2050** for the infra shock while transfers move only
      −0.44 ppt. **⚠ uncommitted, decision pending** — keep `0.01` vs a larger value; the
      "debt-reaction dummy" TODO below is the cleaner fix.
- **Standard-shocks panel redesign + contribution diagnostics** (2026-06-21)
  - `fig:standardShocks` rebuilt as a **4×4 block grid** (was the 3×4 IRF grid of
    raw instruments), one thematic row each: **demand** (output, consumption,
    private investment, government spending), **supply** (labor, human capital, TFP,
    private capital), **nominal** (inflation, nominal interest rate, real
    rate), **fiscal** (primary deficit, transfers, debt service, debt-to-GDP). Units
    differ by row — percent deviation (demand/supply), annualized ppt (nominal), ppt
    of GDP (fiscal) — with per-row y-axis labels; size 15×15 cm. `plotStandardShocksAE.py`
    rewritten; `chartTable.csv` subtitle/note/dims updated; paper prose + `\desc` updated.
  - **`runSimulExport.m` refactored**: per-variable deviation transforms now come
    from `varDict.diffTransf` (`@(x,y)` handles), not a hard-coded pctDev. New
    unit-converted columns for the panel: `PI_ann`/`R_ann`/`rreal_ann`
    (annualized ppt), `pdef_pp`/`by_pp`/`Trans_pp`/`dserv_pp` (ppt of GDP); `Kp`
    added to the percent-dev `varList`. Composite source series `rreal=R/PI`,
    `Trans_ys=Trans/yt`, `dserv=(R-1)*Dt/yt` are injected before the transform.
  - **New model variable** `G = Gc+Igi+Ige+Grd` (total gov spending; declared in
    `declare_all.macro`) and **equation name tags** `[name='yd']` (aggregate demand,
    `model_block.modpart:126`) and `[name='yt']` (production function, line 49). All
    models re-solved to carry the tags (numerics unchanged; only `M_.equations_tags` differ).
  - **Two contribution diagnostics** in `pyScripts/2026-06_wp-imf/tasks.py` (own
    DIAGNOSTICS section, both in the `run_all` pre-list), output → `docs/contributions/`:
    · `plotContributions` (`drivers/plotContributions.m`) — decompose **yd across
      models** (AE ig/cge/cgrd, EM iig/icge), one panel per shock.
    · `investigateContributions` (`drivers/investigateContributions.m`) — decompose
      **across variables for one shock** (currently `yd` + `yt`, `Model_HumanCapital_exp_gc`),
      one panel per target; appends exogenous paths so the production equation's
      allocative `epsiallo_ig` resolves.
    `utils.subroutines.createContributions` requires the target equation to be
    **name-tagged** and **lead-free** (lags OK); to decompose another variable, tag
    its equation and re-solve.
  - `plotStandardShocksAE` reordered to lead the figure list and `run_all` (it is Fig 1).
- **Government consumption as an explicit instrument** (verified 2026-06-21)
  - `Gc = Gcss + ydss*epsi_gc` (`model_block.modpart:108`) — an ordinary instrument with
    its own shock `epsi_gc`, **no longer the budget residual**; public debt `Dt`
    (`model_block.modpart:98`) is the accumulating residual. The paper matches: "Government
    consumption is an ordinary instrument with its own innovation $\varepsilon_t^{gc}$"
    (`draftPaper.tex:584`).
  - **Financing is explicit per experiment** in `runModel.m`'s `modelList`: the `exp_*`
    sims carry only their single instrument shock (debt-financed, no offset); the
    reallocation sims (`epsi_ig`/`epsi_cge`/`epsi_cgrd`/`epsi_cgeCgrd`) add an explicit
    `{epsi_gc, -0.01}` row so the rise is consumption-financed and budget-neutral. No
    programmatic offset loop. *(Resolves the old "public consumption NOT a residual" and
    "wasteful-consumption cut explicit" TODOs.)*
- **Multiplier table finalized** (verified 2026-06-21)
  - `tab:multipliers` is generated by `pyScripts/makeMultipliers.py`, split by spending
    type with AE/EMDE sub-rows (R&D EMDE = `--`), and the financing basis is now stated in
    both text and note — debt-financed expansions of 1 percent of GDP with no
    offsetting cut (`draftPaper.tex:433`, `:447`). The old `% TODO` is gone. *(Resolves the
    "multipliers for simple expansions only" TODO.)* **Superseded 2026-06-26: the expansions
    are now AR(1) ρ=0.9, not permanent — see the DONE entry below.**
- **Adoption-probability rename complete** (verified 2026-06-21)
  - The paper uses $q_t = q_0 (S_t/\Lambda_t)^{\varsigma}$ and $q_0$ throughout
    (`draftPaper.tex:268`, `:323`, `:343`); the only remaining $\lambda_t$ are marginal
    utility and its HC shadow value (correct). *(Resolves the before-circulation
    $\lambda_t \to q_t$ minor.)*
- **Chart sizing finalized** (2026-06-21)
  - `chartTable.csv` carries deliberate per-figure render/display dims — standard-shocks
    15×15, reallocation 7.5×5, efficiency/HC/diffusion 10×6.7, bands 15×10 — and the
    figures render consistently in the compiled PDF.
- **Model equations made consistent with the paper** (wp-equations; verified against current `models/model_block.modpart`)
  - notation: Tier A (`x1/x2`), Tier B (`Ige`/`Dt`/`St` families), the $\lambda/\xi/q$ fix
    (paper-side), `alphaSRD`→`alphaHA`.
  - steady state collected at the end — 12 `var = STEADY_STATE(...)` carry-overs in one
    labelled section (lines 168–179; `6fff063`).
  - variables grouped — shock-driven gov-spending rules grouped; reporting vars under
    "VARIABLES OF INTEREST" (`5fb0e4e`).
  - efficiency processes simplified — dropped unused `rho_Cgrd`/`rhoeffge`/`rhoeff`
    (`f8b742d`, `eb60b11`).
  - efficiency gaps reparametrized as the gap $(1-e^{x})$: $e^{GI},e^{GE},e^{GRD}$
    (lines 139–145; `2ded732`).
  - §4.1 *Transmission of Public Spending* subsection added (lead-in + Fig 1,
    `fig:standardShocks`); Fiscal Multipliers became §4.2.
- **Figure pipeline + task runner** (`cd1c714`)
  - Pipeline: `pyScripts/2026-04_spendingModelling/plot*.py` read
    `docs/csvFiles/figureNumbers_yearly.csv` and write to `<base_path>/figures`. Task runner
    under `pyScripts/2026-06_wp-imf/`: `tasks.py` (invoke), its own `fiscal_config.json`
    (output → `docs/2026-06_wp-imf`), and `chartTable.csv` (chart registry).
  - All six figures regenerated from current data (post model-purge / AE-EMDE efficiency
    re-onboarding). Run via `invoke -r pyScripts/2026-06_wp-imf <task>` (Python 3.11
    framework env).
- **Paper restructure: Model properties vs Policy experiments** (2026-06-19)
  - Split the old §4 *Results* into two sections: **§4 Model properties** (`sec:properties`)
    and **§5 Policy experiments** (kept the `sec:results` label so existing cross-refs resolve).
  - §4.1 *Fiscal multipliers* (transmission narrative + new multiplier table `tab:multipliers`
    + literature benchmarks; absorbs the old "Implied multipliers" para 1).
  - §4.2 *Sensitivity* (flags α_G, μ, efficiency wedges, and diffusion speed as sensitivities;
    the `fig:diffusion` figure was later moved to §5.3 as a policy experiment — see Figure order).
  - §5.1 Reallocation · §5.2 Efficiency gaps · §5.3 *Combining human capital and R\&D*
    (renamed from "Human capital, R&D, and technology diffusion"; absorbs the old
    "Implied multipliers" para 2 on the EMDE combination).
  - Intro section-listing updated. Build: 18 pages, clean (no undefined refs/citations).
  - **Two stubs left:** (i) `% TODO` in `tab:multipliers` — split infra vs human-capital
    multipliers into separate rows once pulled from model output; (ii) financing basis of
    the reported multipliers not stated (single-instrument debt-financed for §4.1 vs
    budget-neutral reallocation for §5.1 is the intended framing).
- **Literature-consistency review** (2026-06-19) → `issues/literature-consistency.md`
  - Checked paper vs CominGertler2006 / Anzoategui2019 / ChangGomesSchorfheide2002.
  - Structure faithful; flags on calibration/attribution: ς=0.8 adoption elasticity (sources
    use ~0.92–0.95; 0.8 is CG's *creation* elasticity); 10-yr lag (CG=10, Anz=5); α_HA=0.45
    "as in CGS" (CGS μ=0.111 qtrly); LBD attribution (CGS tech is exogenous).
- **Section 2 restructure & notation** (`c877750`)
  - Lead-in reworded to "sets out the model's building blocks in turn, starting from households" (was "presents the model by agent").
  - 2.2 retitled "Production and technology"; *Technology creation and adoption* demoted from a subsection to a run-in paragraph inside it.
  - Government subsection retitled "Government: fiscal and monetary policy".
  - Calvo auxiliaries renamed `g_{1,t},g_{2,t}` → `x_{1,t},x_{2,t}` (frees `g` for growth).
  - Trend growth written as constant `g` (was `g_t`).
  - TODO box at top: resolved notation item removed; frame kept (commented-out `itemize` template) for future issues.
- **Wording & clarifications** (`6ce1360`)
  - Public infrastructure capital augments private production as a positive externality.
  - Footnote on $\epsilon$ vs $\vartheta$: Anzoategui et al. fold markup + returns-to-variety into one elasticity; here $\epsilon$ sets the markup, $\vartheta$ only the love-of-variety TFP term $A_t^{\vartheta-1}$.
  - Cross-reference from the technology block to the production function noting $\vartheta$ governs the productivity payoff.
- **R&D efficiency in eq. (Z)** (`ef56a0c`)
  - R&D now enters as the effective wedge $(1-e^{GRD}_t)G_t^{RD}/[(1-e^{GRD})G^{RD,SS}]$ (matches the mod: `Cgrdeff` in creation, raw `Cgrd` in budget).
  - §"Closing spending efficiency gaps" text now lists all three gaps $e^{GI},e^{GE},e^{GRD}$ (the three bars in Figs 3/4).
- **Model source cleanup** (`baf7a94`, model-side)
  - Removed all commented-out equations + two `/* */` blocks from `models/model_block.modpart`.
  - Deleted unused `model_block_ext.modpart`.
  - Active equation set unchanged; all `_results.mat` byte-identical (verified).
- **Model notation aligned to paper** (wp-equations, model-side)
  - Calvo auxiliaries `g1`,`g2` → `x1`,`x2` (paper $x_{1,t},x_{2,t}$).
  - Public HC investment family `Cge`→`Ige`, `Cgess`→`Igess`, `Cgey`→`Igey`,
    `rho_Cge`→`rho_Ige`, `epsi_cge`→`epsi_ige`, `epsiallo_cge`→`epsiallo_ige`
    (paper $I_t^{GE}$ — it is investment, not consumption).
  - `Bt`→`Dt` (paper $D_t$, public debt); `SAt`→`St` (paper $S_t$, adoption expenditure).
  - Touched source: `model_block.modpart`, `declare_all*.macro`, parameter macros,
    `Steady_states_solution.m`, `Model_FR_rate*`, `postSimul.mod`, `varDict.csv`,
    `runModel.m`/`runPlots.m`/`runSimulExport.m`, Jamaica `makeMultipliers.py`, and the
    committed `*.shockValues` (`var epsi_ige;`). Model **filenames/experiment IDs are
    unchanged** (e.g. `Model_HumanCapital_epsi_cge.mod` keeps its name).
  - **Marginal-utility / adoption-prob clash — resolved paper-side** (not a model rename):
    the model keeps the standard `lambda` = marginal utility, so the *paper* was changed to
    match. Paper $\xi_t,\xi_t^H \rightarrow \lambda_t,\lambda_t^H$ (marginal utility, HC shadow);
    paper adoption probability $\lambda_t \rightarrow q_t$ (scale $\lambda_0 \rightarrow q_0$,
    elasticity $\varsigma$ unchanged). Model `lambda`/`lambda_HC`/`probadopt` unchanged →
    no re-run needed for this item. Paper compiles clean (17 pp).
  - ✅ **Re-run done**: re-ran all 52 models (`drivers/runModel.m`) + `drivers/runSimulExport.m`;
    every model solves (rank condition verified), `*-macroexp.mod`/`model/json`/`_results.mat`/
    `figureNumbers*.csv` regenerated with new names, values unchanged. (`Model_FR_rate` is a
    legacy out-of-pipeline model; its `-macroexp.mod` stays stale until run directly.)

## ⬜ REMAINING / TODO
- **New (2026-07-27)**
  - ✅ **Verify the duration-dependent inflation response — DONE (2026-07-30).**
    The former output-level term in the Taylor rule generated the persistent
    deflation after permanent output gains. The rule now follows Gertler and
    Karadi (2011), using minus the log retail-price-markup deviation as an
    output-gap proxy with quarterly coefficient $\gamma_y=0.125$. All 55 configured model
    variants, both sensitivity sweeps, and the paper figures and tables were
    regenerated. The derivation and numerical comparison are documented in
    `investigations/inflation-dynamics/inflationDynamics.pdf`.
- **New (2026-06-28)**
  - ✅ **Verify multiplier consistency with the literature — DONE (2026-06-29).** Full write-up
    in `issues/multiplier-literature-check.md`. Benchmarks: gov-spending multipliers ~0.6–1.0
    and positive (Ramey survey); public investment AE 0.87 / EM 2.29 (Afonso–Rodrigues 2024),
    AE ≈1.5 at 4y (IMF WEO 2014); infrastructure long-run 1 to >2 (Ramey 2020); public-R&D
    social returns 140–210% (Fieldhouse–Mertens). Verdict: the **permanent** multipliers
    (App. D) are the more literature-consistent set — gov consumption is positive and <1
    (the AR(1) version is **negative**, off-consensus), AE infra ≈1.5 matches IMF, and EM>AE
    matches Afonso–Rodrigues; the AR(1) long-horizon investment multipliers (HC 8–10) are
    inflated by the transitory-shock accumulation. R&D being high is supported by the
    R&D-returns literature. ⬜ **Decision pending:** consider swapping so the **permanent**
    table is the headline (Table 3) and AR(1) goes to the appendix, and lead the text with the
    **25-year** (not 250-year) figures. §4.2 wording is already hedged; EMDE infra/HC magnitudes
    are the weakest point and may want a "high end" caveat.
- **New (2026-06-26)**
  - ⬜ **Alternative fiscal-multiplier definitions** — `tab:multipliers`
    (`docs/2026-06_wp-imf/pyScripts/makeMultipliers.py`) currently reports the *cumulative*
    output gain divided by the *cumulative own-spending* injection at 1/5/10/20/25y, for the
    AR(1) ρ=0.9 expansions. Add/compare alternative ways to compute the multiplier:
    (a) impact / peak multiplier (ΔY/ΔG at a single horizon, not cumulated);
    (b) present-value (discounted) cumulative multiplier, Σβ^t ΔY / Σβ^t ΔG (Mountford–Uhlig);
    (c) per-unit-of-debt multiplier (ΔY relative to the debt increase, not own spending);
    (d) fixed-real-rate / no-monetary-response multiplier. Decide which to report and whether
    to add a robustness column or companion table.
- **New (2026-06-24)**
  - ✅ **Table 1 fully reconciled with the model — DONE (2026-06-30).**
    `makeParametersTable.py` now reads **every** parameter cell live from the AE/EMDE
    `gc` `_results.mat`; **no curated literals remain**. Rows and how they resolve:
    α_G (`alphaG`, AE **0.054** un-rounded / EMDE **0.17**), α_HA (`alphaHA`, AE **0.10**),
    α_RD (`alphaRD`, AE **0.0189**), ρ_A (`rho_ZZRD` 0.79), φ (`phiob` **0.98**),
    e^GI/e^GE/e^GRD (`eGI_ss`/`eGE_ss`/`eGRD_ss`), ς (`rhoSADOPT`, AE **0.80**),
    δ (`delta`), μ (`alphaH`), γ (`muy`). The presentation was corrected on
    2026-07-17: EMDE shows **"--"** for inactive technology-creation parameters
    (α_HA, α_RD, ρ_A, e^GRD), while the active adoption elasticity ς=0.10 is
    reported explicitly.
    · **α_HA/α_RD shown EXPLICIT** (the per-quarter AR(1) loadings 0.10 / 0.0189), NOT the
      normalized long-run elasticities — the table NOTE explains the long-run pass-through
      α/(1−ρ_A) ≈ 0.45 and 0.10. This **REVERSES** the earlier "print 0.45/0.10 as curated
      literals" choice (memory `alphaHA-normalization` updated to match).
    · **q₀ REMOVED from the table** — it is **not a model parameter**. The code's scale is
      `kappaprob`, solved in steady state to the targeted adoption probability
      `probadoptss`=0.2/4=**0.05** (→ kappaprob = 0.05/S_ss^ς = 0.31 AE / 0.08 EMDE). q₀=0.1
      is that scale on *detrended* spending, a paper-side reparameterization with no stored
      counterpart; now explained in the §3 calibration prose (`:326`) instead.
    · **φ kept at 0.98** (8%/yr obsolescence = `1−0.08/4`, the principled value; the table's
      old 0.97 was stale — a quick test of 0.97 was reverted, would only shrink the long-run
      R&D/HC multipliers). Table 1 footer switched from `\source` to the aligned `\tabnote{}{}`
      carrying the normalization note.
    Commit trail: `0b7354b` (hybrid start) → `6777acb` (α_G) → `8e7cb51`/`b336d13` (eff gaps
    read + e^GRD recal) → `9164d11` (explicit α) → `81cd45d` (φ) → `d6692a8` (ς "--") →
    `743c00a` (q₀ removed). The two model recalibrations (EMDE α_G, AE e^GRD) are detailed in
    the sub-entries below. (`makeEfficiencyGapsTable.py`/`makeMultipliers` already data-driven;
    `makeNotationTable` is static notation.)
  - ✅ **Efficiency gaps e^GI/e^GE/e^GRD made data-driven; AE R&D gap recalibrated
    0.41 → 0.399 — DONE (2026-06-30).** `makeParametersTable.py` now reads
    `eGI_ss`/`eGE_ss`/`eGRD_ss` from the AE/EMnorm `_results.mat` (EMDE e^GRD kept
    "--", channel off). This corrected a stale **EMDE e^GI 0.42 → 0.41** (model/Table 2
    value 0.406) and exposed that **AE e^GRD (model 0.41) disagreed with the
    data-derived Table 2 median (0.399)**. Fixed by setting `eGRD_ss` 0.41→**0.399**
    in `AE_parameters.macro` (the data median; infra/HC macros already matched their
    medians). Re-ran **all models** (only the 21 AE `_results.mat` changed; EMDE/JAM
    byte-identical), re-exported, regenerated AE figures + tables. **R&D multipliers
    unchanged** (4.89/6.66, 2.35/4.57) — efficiency wedges don't move the marginal
    multiplier, as the paper states; only `efficiencyAE` (R&D-gap closing) shifted
    visibly. Table 1 now e^GI 0.36/0.41, e^GE 0.31/0.33, e^GRD 0.40/--; prose
    `:343`–`:345` updated to match.
  - ✅ **EMDE α_G recalibrated 0.20 → 0.17 (model ← paper) — DONE (2026-06-30).**
    `models/EM_parameters.macro` `alphaG` 0.2→**0.17**, matching the value the
    calibration prose (`draftPaper.tex:322`) already stated; the model was the thing
    out of sync. Re-ran the **18 EMDE models** (`MODEL_FILTER=EM_Model`), re-exported
    (`runSimulExport.m`), regenerated the multiplier tables, `parametersTable.tex`, and
    the two EMDE figures (`reallocationEM`, `efficiencyEM`). **EMDE infrastructure
    multipliers fell** — AR(1) 25y/long 4.54/4.98 → **3.82/4.19**, permanent 2.99/4.34
    → **2.60/3.77** (closer to Afonso–Rodrigues 2.29); EMDE HC and gov-consumption
    barely moved. Updated abstract (`:100`) + intro findings (`:169`) EMDE investment
    range "4.5 to 6.5" → "3.8 to 6.5", and `issues/multiplier-literature-check.md`.
    AE results untouched (no AE `_results.mat` changed). Clean build.
- **New (2026-06-22)**
  - ⬜ **Debt-reaction dummy** — introduce a dummy (0/1 indicator) that switches the
    transfers debt-feedback (`gamma_d_trans*(by-byss)`) **off for the relevant experiment
    window** and back on afterwards, so transfers don't react during the horizon of
    interest while debt still stabilizes in the long run (keeps the rank condition).
    Cleaner than permanently shrinking `gamma_d_trans`, which leaves debt drifting (≈ +57
    ppt by 2050 — see DONE 2026-06-22).
  - ⬜ **Follow-ups from the Frisch result** — mostly moot now that φ=5 is adopted and the
    persistence figures (Figs 9/10) are removed; left as an optional deep-dive: decompose the
    residual gap between our and Galí's ρ=0.9 outcomes into the Taylor `γy` (0.25 vs 0.125) and
    `θ` (0.8 vs 0.75) contributions (the `investigations/galiCheck/` mods can do this).
- **Equations** — (wp-equations) open items only; completed items are in DONE above.
  - ⬜ sensitivity results — §4.3 has qualitative text only (the diffusion-speed figure
    moved to §5.3 as a policy experiment); quantitative sensitivities to $\alpha_G$, $\mu$,
    and the efficiency wedges still TODO.
  - ✅ **R&D timing + (1-ρ_A) in eq. (Z) — RECONCILED (2026-07).** R&D remains
    **lagged** ($(1-e^{GRD}_{t-1})G_{t-1}^{RD}$), matching the model. The earlier
    decision to drop the explicit `(1-ρ_A)` factor was superseded on 2026-07-16:
    equation (Z) again uses $(1-\rho_A)\alpha_{HA}$ and
    $(1-\rho_A)\alpha_{RD}$, and Table 1 reports the recalibrated long-run
    elasticities. The effective per-period loadings and all results are unchanged.
  - ⬜ literature follow-up — decide whether to fix the four flagged calibration/attribution
    items (ς, 10-yr lag, α_HA=0.45, LBD wording); see `issues/literature-consistency.md`
    (the review itself is done — see DONE "Literature-consistency review").
- **Before circulation**
  - Remove the (now-empty) TODO box at the top of `draftPaper.tex` (line ~111).

---

## Files & build
- **Source:** `draftPaper.tex` (single file). Bib: `references.bib` (style `ecta`). Figures: `figures/*.png`.
- **Compile:** `latexmk -pdf -cd docs/2026-06_wp-imf/draftPaper.tex`
- **If it fails with "Runaway argument … `\@newl@bel`" at `\begin{document}`** that's a corrupted `.aux` from an interrupted run. Fix: `latexmk -C -cd docs/2026-06_wp-imf/draftPaper.tex` then rebuild. (The cascade of "Cannot determine size of graphic" errors is a *symptom* of the bad `.aux`, not a figure problem — the PNGs are fine.)
- Current build: **53 pages, clean** (no undefined refs/citations).

## Model behind the paper
- Equations live in **one shared source: `models/model_block.modpart`** (`@#include`d by all 32 `.mod`). The per-model `<name>.mod` are copies of `models/modelTemplate.mod`; `<name>-macroexp.mod` + `model/json/*.json` are generated; `*_results.mat` are the canonicalized outputs.
- Reproducibility verified: re-running `drivers/runModel.m` (all models) + `drivers/runSimulExport.m` leaves every `_results.mat` and `figureNumbers*.csv` **byte-identical**. Headless recipe: `matlab -batch "cd('<repo>'); iniProject; setenv('MODEL_FILTER',''); run('drivers/runModel.m')"` (benign exit-time segfault; verify via log markers "canonicalizeResults: normalized", "runModel total execution time"). `MODEL_FILTER=JAM` runs a subset.

### Paper symbol ↔ `.mod` variable (essential when editing equations)
| Paper | `.mod` | note |
|---|---|---|
| $Z_t$ (created tech) | `ZZRD` | |
| $A_t$ (adopted tech) | `AAt` | enters production as $A_t^{\vartheta-1}$ |
| $\vartheta$ | `varthetaat` | love-of-variety / TFP exponent |
| $\epsilon$ | `epsilon` | markup elasticity (Calvo varieties) — **distinct from $\vartheta$** |
| $x_{1,t},x_{2,t}$ | `x1`,`x2` | Calvo price-setting auxiliaries (mod renamed from `g1`,`g2`) |
| spending instruments $G_t^{C},I_t^{GI},I_t^{GE},G_t^{RD}$ | `Gc`,`Igi`,`Ige`,`Grd` | mod renamed `Cg`→`Gc`, `Ig`→`Igi`, `Cgrd`→`Grd` (`Ige` already matched); SS/share/AR params follow (`Gcss`/`Gcy`/`rho_Gc`, `Igiss`/`Igiy`/`rho_Igi`, `Grdss`/`Grdy`, `Igess`/`Igey`/`rho_Ige`) |
| spending shocks $\varepsilon_t^{c},\varepsilon_t^{gi},\varepsilon_t^{ge},\varepsilon_t^{rd}$ | `epsi_gc`,`epsi_igi`,`epsi_ige`,`epsi_grd` | named `epsi_<instrument>` (suffix = the `Gc`/`Igi`/`Ige`/`Grd` it shocks); allocative shocks `epsiallo_ig`/`epsiallo_ige` left unchanged (not spending instruments) |
| $I_t^{GE}$ (public HC investment) | `Ige` | unchanged (mod earlier renamed from `Cge`); distinct from the stock `Kge` |
| $K_t^{GE}$ (public HC stock) | `Kge` | unchanged (distinct from the flow `Ige`) |
| $D_t$ (public debt) | `Dt` | mod renamed from `Bt`; ratio $d_t$ = `by` |
| $S_t$ (adoption expenditure) | `St` | mod renamed from `SAt` (R&D-labor term `SSt` left as-is) |
| $\lambda_t,\lambda_t^H$ (marginal utility, HC shadow) | `lambda`,`lambda_HC` | paper changed $\xi\to\lambda$ to match the standard mod convention |
| $q_t$ (adoption probability) | `probadopt` | paper changed $\lambda_t\to q_t$ (scale $q_0$) to free $\lambda$ for marginal utility |
| $g$ (trend growth, constant) | `ZZss` (gross quarterly) | |
| $\Lambda_t$ | trend level | |
| $G_t^{RD}$ (raw) | `Grd` | enters the **budget** |
| effective R&D $(1-e^{GRD}_t)G_t^{RD}$ | `Grdeff = (1-eGRD)*Grd` | enters **tech creation** (eq. Z) |
| $\rho_A$ | `rho_ZZRD` | |
| $\alpha_G$ (infra output elasticity) | `alphaG` | mod renamed from `zeta` |
| $\alpha_{RD}$ | `alphaRD` | inactive for EMDEs; implementation value 0, paper value `--` |
| $\alpha_{HA}$ (HC-in-creation, paper) | `alphaHA` | inactive for EMDEs; implementation value 0, paper value `--`; mod renamed from `alphaSRD`; the old dead `alphaHA` param (unused TFP-feedback) was deleted |
| infra/HC/R&D efficiency gaps $e^{GI},e^{GE},e^{GRD}$ | `eGI`,`eGE`,`eGRD` | mod renamed from `effgap`,`effgegap`,`effcgrdgap`; SS params `eGI_ss`,`eGE_ss`,`eGRD_ss`; gap = SS − shock (`epsi_effgi`/`epsi_effge`/`epsi_effgrd`; renamed from `epsi_eff`/`epsi_effcgrd` on 2026-07-01 for consistency with `epsi_effge`) |

## Section 2 structure (current)
2.1 Households · 2.2 Production and technology · 2.3 Government: fiscal and monetary policy · 2.4 Market clearing and equilibrium · 2.5 Balanced growth and solution.

## Derivation notes (`investigations/modelDeriv/`) — style convention
**All derivations live in ONE consolidated note, `modelDerivations.tex`** (user
request 2026-07-02: single big note, easier to handle; the four originals —
costMinimization/pricingEquations/creationAdoption/householdProblem — were merged
into it and deleted). Sections in §2 order: households · retailers (variety
demand + price setting) · cost minimization · creation/adoption (reordered
2026-07-06 with §2.2 and App. A: retailers/price setting before intermediates);
add new blocks as sections there, not as new files. Derivations follow the language and conventions of the Sims NK lecture notes kept in
that folder (`notes_new_keynesian_2024-1.pdf`), per user request (2026-07-02):
- state the problem as an explicit min/max with choice variables under the operator,
  constraint displayed with "subject to:";
- "A Lagrangian for the firm/household is:" + full display; FOCs written
  "∂𝕃/∂x = 0 ⇔ …" under "The FOC are:";
- every algebra step shown, joined by connectives ("Or:", "Simplifying:",
  "Plugging in…", "Dividing the first condition by the second…");
- numbered equations interpreted in plain language immediately after
  ("(N) says that…"); side material in "Aside: …" subsections.
**Notation is NOT Sims'** — keep the paper/model symbols (μ^p, mc_t, ϑ, α_G, Θ, ξ_t…)
so the notes stay consistent with `draftPaper.tex` and the `.mod` files.
Two box devices in `modelDerivations.tex` (both defined in its preamble):
- `\modelcode{...}` — framed box for model-code lines; EVERY equation of
  `model_block.modpart` appears in one exactly once (completeness is stated in the
  intro and the closing sentence — keep it true when the model changes).
- `\intuition{Question?}{Answer.}` — **light-blue box answering a specific question
  the user asked while reviewing the model** (convention set 2026-07-06). When the
  user asks an intuition question about the model and wants it recorded, add it as
  one of these at the relevant spot, phrased as question + answer.
- **Present-value objects are presented SUM-FIRST** (user preference, 2026-07-06):
  state the infinite discounted sum first (the intuitive object), then derive the
  recursion the model carries by splitting off the s=0 term — done for both V and J;
  follow the same order for any future value function added to the note.

## Appendix A presentation conventions (deliberate — NOT TODOs)
- **Self-contained, no main-text equation refs (user rule, 2026-07-02).** Appendix A may
  reference sections/subsections (`Section~\ref{sec:model}`, `subsec:bgp`) but NEVER
  main-text equation numbers (`\eqref{eq:mu}` etc.) — those force the reader to page
  back. Name the equation in words instead ("the bond and capital Euler equations",
  "the production technology of Section 2"). References to the appendix's OWN numbered
  equations (`eq:Astat`, `eq:probadopt`, `eq:sdf`, `eq:VA`, `eq:JZ`, `eq:SAfoc`) are fine.
- **Appendix derives, main text interprets (2026-07-02).** The FOC discussions map
  conditions to their objects in a sentence or two; economic interpretation lives in §2
  and is not restated (e.g., the household FOC discussion is three sentences, keeping
  only the work-vs-schooling arbitrage, which appears nowhere else).
- **Hybrid level/stationarized presentation.** Appendix A states the household,
  production, price-setting, fiscal, and market-clearing conditions in **level form**
  (matching the main text), but the **technology block** (`eq:Astat`–`eq:SAfoc`) is
  stated **in stationarized form**, because the detrending factor $1+\gamma^a$ appears
  in those conditions themselves. This is intentional. **The detrending discussion
  lives in §2.5 ONLY** (user request 2026-07-02: don't elaborate on detrending in the
  appendix) — §2.5 ends with the note that App. A's technology block is the one
  stationarized departure; App. A itself carries just one intro sentence + short
  pointers to `subsec:bgp` (after `eq:Astat` and `eq:probadopt`). Do not "harmonize"
  the blocks to a single convention, and do not re-expand detrending prose in App. A.
- **Adoption probability, two equivalent forms.** App. A writes $q_t = q_0 S_t^{\varsigma}$
  (with $S_t$ already detrended); §2.2 writes $q_t = q_0 (S_t/\Lambda_t)^{\varsigma}$
  (level form). Same object under the two conventions above — not an inconsistency.
- **Debt notation is $B_t$ everywhere** (no $D_t$ in the paper). With a single
  instrument (government bonds), household bond holdings equal government debt issued,
  so the same symbol serves both (`draftPaper.tex:308` defines $B_t$/$b_t$); the .mod
  variable is still `Dt`.

## Section structure (current, post-2026-06-19 restructure)
§1 Introduction · §2 The model · §3 Calibration · §4 Model properties (4.1 Transmission of public spending · 4.2 Sensitivity · 4.3 Fiscal multipliers · 4.4 Improving efficiency) · §5 Policy experiments (5.1 Reallocation · 5.2 Combining HC and R&D) · §6 Conclusions · App. A Equilibrium conditions · App. B Notation: paper and model symbols (`app:glossary`, generated `glossary{Endogenous,Parameters,Exogenous}.tex`) · App. C Spending-efficiency estimates (efficiency-bands figure) · App. D Government Consumption: Robustness to Model Structure (`fig:simplifiedGc`) · App. E Permanent Shocks (`fig:standardShocksPerm` transmission + `tab:multipliersPerm`).

## Figure order (for `\ref`/numbering)
All figures use `[H]` (non-floating, by request — accepts the odd page-bottom gap rather
than letting figures drift). Reallocation keeps a combined side-by-side (subpanels a/b);
the EMDE efficiency chart remains reproducible but is temporarily excluded from the draft:
Fig 1 transmission of standard shocks — AE 5×4 block grid (demand/productive stocks/labor/prices and rates/fiscal rows, mixed units by row; §4.1, `fig:standardShocks`; script `pyScripts/2026-06_wp-imf/plotStandardShocksAE.py`) · Fig 2 sensitivity to structural parameters (§4.2, `fig:sensitivity`) · Fig 3 matched permanent spending and efficiency improvements, a 4×3 matrix with instruments in columns and selected outcomes in rows; solid lines are AE spending benchmarks, dotted lines are matched AE efficiency improvements, and dashed lines are matched EMDE efficiency improvements (§4.4, `fig:efficiencyAE`) · Fig 4 reallocation — (a) AE, (b) EMDE (§5.1, `fig:reallocation`) · Fig 5 HC + R&D mix (§5.2, `fig:humanCapital`) · Fig 6 technology diffusion (§5.2, `fig:diffusion` — a policy experiment using the HC/R&D mix under different diffusion speeds) · Fig 7 efficiency bands by income group (App. C, `fig:efficiencyBands` — 2×2 panels: infra, health, education, R&D; R&D plotted for AEs only; script `pyScripts/2026-04_spendingModelling/plotEfficiencyBands.py`).
Tables: Table 1 parameters (§3, `tab:parameters`) · Table 2 efficiency-gap derivation (§3, `tab:efficiencyGaps` — main-text table; AE median; EMDE = avg(EM,LIC); HC = avg(health,education); rows: infra, health, education, HC, R&D (AE 0.399, EMDE shut down); 2023 medians from the efficiency database; note points to App. C Fig 7; ported from the Jamaica deck's calibration slide, Jamaica column + previous-FM parentheticals removed) · Table 3 multipliers by horizon (§4.2, `tab:multipliers` — debt-financed AE **and** EMDE expansions of the 7 `exp_*` sims at 1/5/10/20/25y, grouped by spending type with AE/EMDE sub-rows; R&D EMDE = `--`; generated by `pyScripts/makeMultipliers.py` → `multipliersTable.tex`, \\input by the paper; own-spending denominator so consumption is finite; **replaced** the old approximate AE/EMDE placeholder) · glossary tables (App. B, `tab:glossEndo`/`tab:glossParam`/`tab:glossExo` — generated `glossary*.tex` longtables, replaced the old Table 4 notation table in App. A).

## Writing style — drafting the paper (esp. the introduction)
Follow Claudia Sahm, *Writing Economics Research Papers* (2021), in
`literature/ClaudiaSahm_WritingEcon.pdf` — a style reference for drafting, **not a
citation** (do not add it to `references.bib` or cite it in the text).

**How the introduction should be written.** Each element below is **one paragraph**
in the introduction (and **one sentence each** in the abstract), roughly in order:
1. **Motivation** — why the question matters. Set the stage with *facts*; do not just
   assert that it is important.
2. **Research question** — state one *specific* question ("This paper studies / asks
   whether …").
3. **Contribution** — what answering the question teaches us. **Lead with our own
   contribution**, then place it in the context of others. The contribution paragraph
   *substitutes for* a related-literature section; if one is unavoidable, keep it short.
4. **Methods** — how the question is answered: identification and the key features of
   the approach.
5. **Results** — the main findings, **no more than three**, stated clearly and with
   *numbers*. These are the payoff — take the space to explain them.
6. **Road map** — a short by-section description of the rest of the paper.

**Make contribution and findings shine — they are the most important parts.**

General style rules to apply throughout:
- First sentence of each paragraph makes its main point; read in sequence, the opening
  sentences should tell the paper's story.
- Lead with the economic purpose, interpretation, or result before introducing notation
  or implementation details. Do not open with auxiliary variables, equations, or model
  mechanics when a plain-language statement can orient the reader first.
- Do not open a paragraph with descriptive framing such as ``Table X reports'' or
  ``Figure X shows.'' State the result or process first and introduce the cross-reference
  parenthetically or naturally within the sentence.
- Keep each sentence focused on one main claim. Treat about 30 words as a review
  threshold: split longer sentences, especially those with multiple independent or
  subordinate clauses, unless keeping them together materially improves clarity.
- Stick to facts; avoid subjective words ("surprising", "important", "obvious") and
  limit adverbs.
- Emphasize *economic* significance over statistical significance.
- No jargon without first defining it (readers come from other sub-fields).
- Figures/tables must stand alone (no acronyms/jargon/symbols), with labelled axes,
  legible fonts, and legibility in black and white; keep a "money chart" for the
  headline result.
- Do not apologize for what the paper does not do.

*State of the §1 intro (revised 2026-06-19):* now follows the Sahm arc and is
**contribution-led** — motivation → specific question + our contribution (others in
context) → compact model/methods paragraph (the former strand-by-strand lit review
folded in, all citations kept) → three findings → road map.

## IMF Editorial Style Guide (house style)
Reference: `literature/IMF_EditorialStyleGuide_2e.pdf` (2024, 2nd ed.) — IMF house
style for the WP. **Not a citation** (do not add to `references.bib` or cite). Rules
that bear on this paper:
- **Numbers:** spell out < 10, numerals for ≥ 10 (mixed → numerals); always numerals
  for percentages, ratios, decades, currency-with-symbol, and millions and above.
  Use the word "percent" in text, not "%".
- **Ranges & punctuation:** en dash for ranges (2026–31; 6–8 percent). Use the serial
  (Oxford) comma. Hyphenate phrasal adjectives before a noun (long-run output,
  debt-to-GDP ratio, spending-efficiency gap).
- **Cross-references:** capitalize in-text refs to Figure / Table / Section /
  Appendix / Box, but **not** numbered equations or figure panels — "equation (2.10)",
  "panel a" (lowercase). Use `\eqref{}` for equations.
- **Titles:** headline-style (Title Case) for section, figure, and table titles
  (matches CLAUDE.md "Title Case for chart titles").
- **Country terms (lowercase in running text):** advanced economies; emerging market
  and developing economies (EMDEs); low-income (developing) countries. Avoid "advanced
  countries" and "emerging economies". GDP and R&D always as acronyms.
- **Currency:** "$" is the assumed US dollar; use "US$" only to disambiguate. "US",
  never "U.S.".
- **Figures/tables:** Source/Sources line first, then a "Note:" line; titles
  headline-style; must stand alone (no undefined symbols/jargon).

*Compliance status of draft (2026-06-19):*
- Already OK: "percent" word; en-dash ranges; lowercase country terms; GDP/R&D
  acronyms; capitalized Figure/Table/Section/Appendix refs; lowercase "panel a".
- **Fixed:** "ten years"→"10 years"; "emerging economies"→"emerging market economies";
  capitalized "(Eq.~\ref…)"/"(Eqs.~…)"→`\eqref` / lowercase "equations"; all section,
  figure, and table titles set to headline-style (Title Case); `\desc` macro now prints
  "Note:" (was "Notes:").
